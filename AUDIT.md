# RHRC formal audit — theorem authority through PR #163; research evidence through PR #172

> **RH remains OPEN.**

## Authority split

Live GitHub head + exact compiler/CI evidence outrank this prose.

```text
THEOREM AUTHORITY
latest theorem-bearing PR = #163
validated theorem head = b418ff034428f92594bab0e5b8276181a086ee4b
validated theorem tree = c397b3a015ea54e38ecfe626d6e29556fe963839
RHRC #1039 = SUCCESS
Permansson #812 = SUCCESS

LATEST RESEARCH-EVIDENCE ANCHOR
merged research PR = #172
validated research head = 4c857cd031497d895232a18a4bfb9a094d9facae
merged research commit = a31bb0bb7f025d7727dd3f224c705af797f64a19
research tree = c64b098c3159d739fa15eeaa96e35693615873d7
RHRC #1063 = SUCCESS
Permansson #836 = SUCCESS

CONTROL SEMANTIC AUTHORITY
PR #117 merge = 19346f4c00d13bf33db95cbe5325233f86e54c12
selected first break = E4A4-SCHUR-FB-05
```

The theorem and research anchors are intentionally different. No result from #165-#172 is promoted to Lean theorem authority merely because those PRs are green.

## Exact theorem progression

### #153

Retained whole-cell regular first-bad ancestry and negative canonical source energy; exact finite pole-prime discrepancy/full-channel normal form.

### #155

Finite discrepancy integrability; anchored Riesz primitives; AC/a.e. derivative seam; legal arbitrary-order conditional integration by parts; source oddness and all even endpoint jets.

### #157

Genuine complex production D transport; boundary-flat jets through 6; even jets through 8; exact complete Riesz 6/even 8; retained transformed negativity; off-line-zero -> retained Riesz-6 negative certificate.

### #159

General moment-prefix odd-jet law, exact seventh/even-ninth self-energy leading-moment formulas, generic signed Riesz boundary recurrence and retained R6->R7 / even R8->R9 exact moment-square decompositions.

### #161

Same-state shifted negative secular trial; strict even R8 negativity; exact R9/M4 boundary inequality; cross-parity `Gamma * explicitCanonicalSourceMoment` identity; odd-good source-moment nonvanishing; odd-bad OR source-moment-nonzero fork.

### #163

Lean theoremizes the exact mixed quadratic-normal source observable and its retained-state composition:

```text
h_v^(7)(0) = -2*(2*pi)^6*M4(v)
|h_v^(7)(0)|^2 = 4*(2*pi)^12*|M4(v)|^2
finite-prime term of explicitCanonicalSourceMoment samples the same h_v
2*(2*pi)^4*(R8(v)-R9(v)) = S8(L)*|h_v^(7)(0)|^2
```

Retained specialization additionally yields the strict R9 upper bound without assuming a sign for `S8` and proves `crossParityGamma != 0` under opposite-parity goodness.

## What theorem authority does not establish

No current Lean theorem establishes:

```text
explicitCanonicalSourceMoment != 0 -> M4 != 0
M4 != 0 -> explicitCanonicalSourceMoment != 0
finite production samples determine h^(7)(0)
canonicalPolePrimeRieszEndpointScalar L 8 >= 0
canonicalPolePrimeRieszEndpointScalar L 8 != 0
boundary-flat threshold-entry law as a separately theoremized aperture theorem
Schur-envelope derivative as a project theorem
threshold-to-threshold integrated Schur barrier
q13/N2/K3/even whole-cell positivity
universal sign of arithmetic threshold entry lift
simultaneous even+odd bad exclusion
odd-selected first-bad branch closure
negative-root exclusion
RiemannHypothesis
```

## Research audit after theorem authority

### PR #165 — endpoint scalar

The executable Riesz-8 endpoint scalar is independently reconstructed with the exact repository normalization and von Mangoldt prime powers. Broad finite searches find no sampled negative value.

**Audit conclusion:** this is not a global sign theorem; positivity alone propagates retained negativity rather than contradicting it.

### PR #166 — shifted same-state discriminator

The finite discovery state is corrected to `u_lambda = c - W(H-lambda G)^(-1)r`. A broad scout finds zero reconstructible negative shifted states but isolates a near-critical odd `Q16/N3/K4` family. The strongest sampled point is independently Arb/Sylvester-certified positive.

### PR #167 — Q16 full-cell scalar barrier

Floating full-cell optimization finds no negative aperture and pushes the apparent minimum toward `log 17`. The direct whole-cell Arb representation remains unresolved in every depth-8 leaf: 256/256 `UNRESOLVED`.

**Audit conclusion:** direct dependency-heavy full-matrix subdivision is methodologically insufficient in the current representation. `UNRESOLVED` is not sign evidence.

### PR #168 — threshold moment jet / Q17 microscope

Boundary-flat parity analysis gives exact executable first surviving orders 7 (`M3^2`) in odd parity and 9 (`M4^2`) in even parity. The exact log17 threshold and all 18 two-sided Arb microscope points certify positive. Floating discovery in Q17 reports `CONTINUES_DOWN_BUT_POSITIVE`.

### PR #170 — theorem-aligned Schur visibility / background drift

The selected scalar is rebuilt in theorem-aligned `[W|c]` one-step coordinates. For q17/N3/K4/odd, Arb certifies predecessor positivity at the threshold and nonzero Schur visibility. The exact entering-q pivot effect is positive at checked offsets while the q-removed background finite difference is negative.

**Audit conclusion:** the isolated q17 arithmetic kick is visible and favorable, but it does not determine complete pivot motion.

### PR #172 — threshold-to-threshold barrier dynamics

The executable research layer broadens the test to genuine arithmetic intervals and multiple q/N/parity targets. No sampled bad successor is found, but the universal "arithmetic replenishment" heuristic is falsified as a research default.

Finite Arb replay includes both signs for the current-q entry lift:

```text
q9 / even   NEGATIVE_CERTIFIED
q13 / even  NEGATIVE_CERTIFIED
q16 / odd   POSITIVE_CERTIFIED
```

The most dangerous sampled target is

```text
q=13 -> 16, N=2, K*=3, parity=even.
```

At the quantized near-minimum Arb certifies

```text
full unit-shell pivot      ~= +5.8401616e-12
q-removed background       ~= +1.2217611e-11
q13 entry lift             ~= -6.3774491e-12
```

with H1 predecessor positivity certified.

The q17 near-end replay also demonstrates a separate scope firewall: the full physical state can remain H1 while the q-removed background is not H1-certified, so the algebraic lift does not automatically inherit first-bad comparative meaning.

The threshold-barrier diagnostics additionally expose channel cancellation ratios around `1e9`-`1e10`. Coarse sums of component signs are therefore unsafe at a `1e-12` residual scale.

**Audit conclusion:** the next representation should target the complete q13/even scalar, not a universally favorable arithmetic kick.

## Current post-green frontier

At `N=2 -> K*=3`, the exact executable theorem-aligned carrier is 1D -> 2D. Write

```text
H(L) = [[a(L), b(L)],
        [b(L), d(L)]].
```

Then

```text
H1 <-> a(L) > 0
P(L) = d(L) - b(L)^2/a(L)
Delta_2(L) = a(L)d(L) - b(L)^2
```

and therefore, in H1 scope,

```text
sign P(L) = sign Delta_2(L).
```

The highest-information next research problem is to resolve the q13/N2/K3/even determinant barrier across `log13 <= L <= log16`.

The rigorous backend must respect the physical cutoff subcells

```text
[log13,log14], [log14,log15], [log15,log16]
```

unless exact inertness of the zero-von-Mangoldt Q=14/15 additions is separately certified.

This low-dimensional scalar route is an allowed escape from the #167 dependency-heavy full-matrix interval failure. It is not a revival of brute subdivision.

A positive q13 whole-cell certificate would be a method/structure result, not FB-05 closure: the exact retained first-bad state from #153/#161 is not restricted to that one finite cell.

## Claim firewall

- #165-#172 are research evidence, not theorem promotion.
- exact executable algebra is not Lean theorem authority.
- finite Arb certification is not automatically a whole-cell or global theorem.
- finite central differences/quadrature are not derivative theorems.
- absence of sampled negative states is not positivity.
- endpoint-scalar positivity alone is not first-bad exclusion.
- current-q arithmetic entry lift is sign-indefinite in the tested finite states.
- physical H1 does not imply q-removed-background H1.
- q13 whole-cell positivity alone would not close FB-05.
- channel pivot values cannot be summed as if the Schur map were linear.
- large cancellation ratios require cancellation-preserving arithmetic.
- `UNRESOLVED` interval output is not sign evidence.
- sourceMoment nonzero does not imply `M4` nonzero, nor conversely.
- simultaneous parity badness and odd-selected coverage remain open.
- negative-root exclusion remains open.
- **RH remains OPEN.**
