# Post-#231 obstruction ledger delta — retained source Gram frontier

> **RH remains OPEN.** This ledger records exact theorem authority and the next
> obstruction after merged-green PR #231.

## What became formally true

PR #231 closes the previously open source-coupling bridge and retained source
balance. The exact generic correction functional is expressed through
`cubicShellCoupling` and production source channels. On the selected-even /
odd-good retained branch the exact same-state scalar identity
`q - J = M4 - S*C` is proved.

## Workflow harvest

The exact #231 head completed 11/11 attached workflows GREEN. CCM,
ExceptionalZero, claim/regression, Control-v2, normalization, no-placeholder,
Permansson, and every attached historical research replay passed. Historical
replays retain their previous evidence classifications.

## What changed

`post229_source_coupling_bridge` and `post229_retained_source_balance` are
now CLOSED / PROVED BY #231. OBS-059I remains OPEN: the new equality constrains
the retained state but does not itself exclude it.

## Upstream implications

The #229 Riesz representation is now a consumed prerequisite rather than the
active frontier. Re-deriving alpha/Gamma quotient identities is no longer a
highest-information action.

## Downstream implications

The active descriptive target advances to
`RETAINED_CROSS_PARITY_SOURCE_GRAM_DISK`.
Required new information is
`GOOD_SECTOR_GRAM_CONTROL_COMPOSED_WITH_RETAINED_SOURCE_BALANCE`.

The intended endpoint is not merely `canonicalOneStepDomination`; it is a
same-state quantitative region for the variables already tied together by #231.

## Resurrected routes

Good-sector Gram geometry is conditionally resurrected as a bridge on the
distinguished resolvent direction. Universal domination as an independent
arithmetic mechanism remains downgraded because the Action Registry records that
successor-positivity restatement alone has no FB-05 information gain.

## New RH-relevant clues

**LEAD / HYPOTHESIS.** A retained disk
`|q - M4 + S*C|^2 <= E_c * Re C` may shrink the admissible selected-even /
odd-good counterexample region enough to compose with #209/#213.

It is not currently proved and must be falsified aggressively once formalized.

## Falsification checks

A disk derived entirely from odd-good positivity can be mathematically true yet
information-redundant. Test redundancy against existing same-state inequalities
before treating it as progress toward exclusion.

The generic simultaneous odd-bad branch remains OPEN. The odd-selected first-bad
branch remains OPEN. Parity-complete retained-state exclusion remains OPEN. The
terminal Mathlib RH seam remains OPEN.

## Highest-leverage next moves

Prove the generic Gram interface, specialize to `R a`, derive the retained
disk, and then determine whether the accumulated retained constraints have empty
intersection. If not, use the surviving region to identify the smallest genuinely
independent canonical arithmetic restriction still missing.

## Standing questions

Does #231 plus good-sector Gram geometry eliminate any retained states that were
previously admissible?

If not, which degree of freedom survives the disk?

Can that surviving freedom be attacked by the complete source-kernel or Riesz
boundary machinery without circularly assuming the desired successor positivity?

**RH remains OPEN.**
