# Post-#190 research delta — joint selector separability and canonical realizability

> **Claim firewall: RH remains OPEN.**
>
> This is a research-state delta. It does not promote exact-rational/executable research evidence to Lean theorem authority.

## Exact authority checked

```text
THEOREM AUTHORITY
PR #184
validated theorem head = a756494ebe7e2530715e996b9a9a341fbe07c683
merged theorem commit = 6f04e94473eb112b59eeaa2c6fc6ac6cbe7fe30e
validated theorem tree = 6c77cd470809959a403b3bcc5f08d39f4076fa4c

RESEARCH AUTHORITY
PR #190
validated research head = 8701920b0da18ae6595ad0eee55c1f6cb291a94f
merged research commit = f87da9fde71dd1e74419c6ae5848eee3787c27e4
validated research tree = af8774b65c898de221a5bf32977ccff3407a7b2d
RHRC #1096 = SUCCESS
Permansson #869 = SUCCESS
research disposition = JOINT_EXACT_VECTOR_SEPARABLE / JOINT_THRESHOLD_SIGNATURE_SEPARABLE

CONTROL AUTHORITY
PR #117
selected first break = E4A4-SCHUR-FB-05
terminal claim = RH_OPEN
```

# What became formally true

No new Lean theorem became true in PR #190. Formal mathematical authority remains exactly PR #184.

The #184 theorem package still proves the complex-Hermitian N2 Schur calculus, frozen production log-cover decomposition, fixed-cell bridge, predecessor/canonical-shell geometry, algebraic envelope derivative split

```text
P_t' = -E + R_t'
```

and the conditional implication

```text
R_t' < E -> P_t' < 0.
```

PR #190 does not change those statements or their assumptions.

# What became research-certified

The research chain leading to #190 is:

```text
#186 broad source-remainder domination -> DOMINATION_SIGNAL_MIXED
#188 frozen normalization-safe selector audit
#189 coordinate-by-coordinate exact semantic independence
#190 joint exact semantic independence of the full seven-selector vector
```

PR #190 reuses the exact normalized algebra from #189 and certifies a deterministic exact-rational reflected pair. Across the pair:

```text
G1 coupling ratio                         identical
P1 parity predecessor ratio              identical
P2 parity log-slope gap                  identical
C1 prime directional contribution        identical
C2 arch directional contribution         identical
C3 pole directional contribution         identical
C4 prime-vs-smooth squared ratio         identical
all seven frozen threshold relations     identical and nonboundary
normalized FB-05 target sign             opposite
```

The executable classifications are:

```text
JOINT_EXACT_VECTOR_SEPARABLE
JOINT_THRESHOLD_SIGNATURE_SEPARABLE
```

Because the complete seven-dimensional observation vector is identical, all `2^7 - 1 = 127` nonempty coordinate subsets are also identical across the same target-flipping pair. This is logical closure from one exact witness, not 127 fitted searches.

The target-containing positive controls `channel_sum`, `direct_full_ratio` and `mechanism_sum` still distinguish the pair with exact reconstruction identities. The audit therefore remains sensitive to actual target information.

# What changed

Before #190, one remaining possibility was that several individually insufficient selectors might jointly determine the FB-05 target sign.

After #190, that possibility is closed for the frozen strong observation surface in the audited ambient normalized algebra.

The project should therefore stop asking

```text
which function of G1/P1/P2/C1/C2/C3/C4 recovers the target sign?
```

and instead ask

```text
which exact canonical production constraint prevents the ambient reflected twin from being a real arithmetic CCM state?
```

This is a change from **classification** to **realizability geometry**.

# Upstream implications

1. **The ambient normalized state space is too large.** #190 demonstrates a real information loss in the chosen observation map.
2. **Do not add selector algebra without new production content.** Another combination or threshold over the same seven observables cannot recover information already lost by the full vector.
3. **Production definitions now matter as constraints.** Source channels, Schur geometry and aperture derivatives must be generated by one common canonical state rather than treated as independent coordinates.
4. **Formal theorem investment should follow the realizability audit.** The next Lean lemma should be whichever exact production relation actually excludes the twin, not whichever relation is easiest to formalize.

# Downstream implications

The live Pair-A route is now:

```text
canonical production realizability restriction
  -> reflected ambient branch impossible / useful contact property forced
  -> #184 contact/log-drift calculus
  -> independently theorem-backed first-bad property
  -> same-state contradiction
```

If no exact canonical restriction removes the twin, Pair A loses priority and the alternative incompatibility pairs become more valuable.

The post-#190 result also compresses future falsification: one need not test the 127 subsets separately. Any proposed successor to the selector programme must add genuinely new production information.

# Resurrected routes

Several routes become more relevant because #190 identifies the failure as an ambient-vs-canonical gap rather than merely a poor threshold choice:

- **common-aperture trajectory rigidity:** actual states lie on a low-dimensional aperture trajectory rather than filling the ambient normalized algebra;
- **source-channel integrability/coupling:** pole, arch, prime and scalar channels are generated together from one canonical source;
- **centered H1 / local injectivity:** if a production observable is locally injective near the dangerous region, equal full selector vectors may force equal apertures;
- **two-parity squeeze:** opposite-parity data may add genuinely new canonical information rather than another same-parity selector.

Global Schur monotonicity is not resurrected.

# New RH-relevant clues

## LEAD — the #190 reflection exploits a non-production degree of freedom

The current executable source derivative decomposition constructs

```text
scalar_shift = 2*cCorrection'(L) I
arch_signed  = -arch_direct - scalar_shift.
```

Therefore

```text
arch_signed + scalar_shift = -arch_direct
```

at matrix level, and the same linear relation survives the common parity restriction and directional functional.

The #190 reflected pair changes the hidden scalar channel while holding the observed arch channel fixed. That is legal in the ambient algebra used for the semantic-independence audit, but it is not obviously realizable by one canonical source family.

**Status:** LEAD / executable-definition consequence. It is not yet a Lean theorem excluding the reflected class.

## LEAD — canonical states may occupy one sheet of a two-sheet ambient ambiguity

C4 sees the smooth channel only through a square. The ambient algebra therefore admits a sign-reflection ambiguity. Actual production may select or restrict one sheet through common-aperture/source-channel relations.

The useful theorem, if this clue survives, would be a branch-selection/realizability statement rather than another empirical selector inequality.

## LEAD — local trajectory injectivity may be enough

If two actual apertures cannot share the complete seven-vector in the dangerous local cell, then the ambient reflected pair cannot be realized by two distinct production states. A local injectivity theorem for one production coordinate may therefore be sufficient even if no pointwise algebraic identity alone closes the ambiguity.

# Falsification checks

Attack the realizability clue before building around it:

1. **Exact replay:** verify the #190 pair against each proposed production identity and record the first failure.
2. **Positive-scalar test:** do not confuse exclusion of the specific negative-scalar witness with exclusion of the whole reflected class.
3. **Arch/scalar coupling test:** impose the actual coupling while allowing all other ambient coordinates to vary; check whether another exact reflected pair survives.
4. **Common-aperture test:** require both source channels and Schur geometry to arise from the same `L` rather than independently chosen normalized values.
5. **Trajectory test:** search/adversarially solve for `L1 != L2` with identical full seven-vectors and opposite target signs.
6. **Target-leakage test:** no new constraint may contain the target, `rho`, margin or full pivot derivative in disguise.
7. **Normalization test:** the exclusion must survive allowed basis/shell normalizations.
8. **Search-vs-proof test:** failure to find a twin is `UNRESOLVED`, not proof of nonexistence.
9. **Same-state test:** any RH-directed contradiction must eventually attach to the exact retained state/aperture/parity/normalization/production object.

# Highest-leverage next moves

1. **RESEARCH:** build the post-#190 canonical-realizability audit. Reuse the exact #190 witness and impose production constraints layer by layer.
2. **RESEARCH:** make the output classify `EXACT_TWIN_SURVIVES`, `EXACT_TWIN_EXCLUDED_BY_IDENTITY`, or `UNRESOLVED` at every layer.
3. **RESEARCH:** if pointwise constraints do not close the twin, test common-aperture trajectory injectivity rather than adding more selectors.
4. **THEOREM:** formalize only the smallest exact production relation that the audit shows actually removes the ambiguity.
5. **COMPOSITION:** combine that theorem with #184 and an independently proved opposing first-bad property on the same state.
6. **FALLBACK:** if a genuine canonical twin survives full production constraints, downgrade Pair A and prioritize negative-index/sampling and two-parity routes.

# Standing questions

> Given everything that is now formally true, what becomes possible that was not possible before?

We can stop treating the missing FB-05 information as an unknown classifier over the frozen selector vector. The search can now focus on the geometry of the canonical production image inside the larger ambient algebra.

> If this contains a clue toward RH, where does that clue propagate upstream or downstream through the existing mathematics?

Upstream, it points to production definitions and common-aperture identities that should be simplified/theoremized. Downstream, a realizability exclusion can feed directly into #184's contact calculus and the same-state first-bad contradiction programme.

> What experiment, lemma, reformulation, or connection would most efficiently tell us whether that clue is real?

Take the exact #190 reflected pair and ask which canonical production constraint fails first. Then test whether that constraint excludes the entire reflected class rather than only the chosen witness.

**RH remains OPEN.**