# RHRC research leads delta after PR #203 — Q14 composite parity-gap falsifier

> **Claim firewall: RH remains OPEN.**

This is the current post-green research delta for merged-green PR #203. It records research evidence and routing consequences only. It does not promote a Lean theorem, close FB-05, exclude a negative root, or prove RH.

## Exact authority state

```text
THEOREM AUTHORITY
PR #184
head  = a756494ebe7e2530715e996b9a9a341fbe07c683
merge = 6f04e94473eb112b59eeaa2c6fc6ac6cbe7fe30e
tree  = 6c77cd470809959a403b3bcc5f08d39f4076fa4c

LATEST RESEARCH EVIDENCE
PR #203
head  = c8196830a8b49e657b28d36b364e1cff68c568d6
merge = ab660e812a78d482145eadc3e42d186a63fa812b
tree  = 7360e366fe8d623ef63ca902c23522bb72935848
research disposition = COMPOSITE_PARITY_GAP_PATTERN_FALSIFIED

CONTROL SEMANTIC AUTHORITY
PR #117
selected first break = E4A4-SCHUR-FB-05
terminal claim = RH_OPEN
```

# What became formally true

**PROVED:** no new Lean theorem. PR #184 remains theorem authority. Its exact Hermitian/contact/log-cover infrastructure, including `M~(t)=-tI+R~(t)`, remains unchanged.

The #203 implementation does not promote executable identities into Lean theorem authority.

# What became research-certified

The exact inherited ancestry replays:

```text
#197
GLOBAL_MONOTONE_ORIENTATION
uniform orientation = J_POSITIVE

#201
DISCREPANCY_REPRESENTATION_DEPENDENCY_UNRESOLVED
```

The repaired #203 parity-contrast implementation validates:

```text
exact rational contrast conversion
scalar identity cancellation before interval transport
G   = O - E
G'  = O' - E'
G'' = O'' - E''
```

The preregistered center-level mechanism was

```text
E > 0
G > 0
G' >= 0
E' <= 0
```

and the certified result is

```text
center_kill_switch_survives = false
full_cover_executed = false
completed_leaf_count = 0
mechanism_classification = COMPOSITE_PARITY_GAP_PATTERN_FALSIFIED
control_transfer_status = PATTERN_FALSIFIED_BEFORE_FULL_COVER
theorem_promotion = false
fb05_closed = false
negative_root_exclusion = false
rh_claim = false
```

The exact research claim is that the complete preregistered conjunction fails its required six-center gate. The summary does not justify assigning a particular opposite sign to any one primitive quantity.

# What changed

Before #203, Pair A had one authorized higher-level escape left after the #199 and #201 representation failures: preserve the assembled parity object and test a simple preregistered composite sign law.

PR #203 consumed that escape cleanly. Therefore the planned Pair-A **representation-engineering programme** is now consumed/downgraded as the default tactic.

This is not the claim that every future Pair-A theorem is false. A Pair-A revival requires genuinely new theorem-level structure selected independently of the failed target data.

The initial red #203 run was an implementation failure, not a mathematical falsification: an integer-only SymPy-to-Arb helper truncated noninteger rational contrast entries. The repair uses exact rational conversion and adds independent normalized `O-E` reconstruction checks. The green falsification therefore occurs only after the implementation/provenance firewall is restored.

# Upstream implications

1. Exact rational research geometry must preserve rational numerator/denominator data when transported into Arb. Integer-only conversion is forbidden for a noninteger contrast matrix.
2. Shared comparator paths are insufficient validation when both can share the same bug. The independent `G=O-E`, `G'=O'-E'`, `G''=O''-E''` path is reusable validation infrastructure.
3. The theorem-backed common scalar cancellation remains mathematically legitimate infrastructure even though the proposed sign mechanism fails.
4. Do not fit a replacement `E/G/G'/E'` sign conjunction to the now-unblinded six centers.

# Downstream implications

The direct bounded fact remains:

```text
#197 -> GLOBAL_MONOTONE_ORIENTATION / J_POSITIVE
```

but extracting a simple Pair-A mechanism has now failed through several independent design layers:

```text
#186 broad remainder domination -> DOMINATION_SIGNAL_MIXED
#190 frozen selector composition -> jointly separable in ambient algebra
#199 source decomposition -> SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED
#201 cancellation-preserving discrepancy pairing -> DISCREPANCY_REPRESENTATION_DEPENDENCY_UNRESOLVED
#203 preregistered full-composite sign law -> COMPOSITE_PARITY_GAP_PATTERN_FALSIFIED
```

The next research question should therefore change mathematical families rather than change representations of `J`.

# Resurrected routes

## Pair D — same-state two-parity squeeze

Pair D is now the primary active route because the project already has same-state parity/source/Riesz infrastructure through #159/#161/#163.

The intended composition is:

```text
one exact retained canonical first-bad state
  -> selected parity bad
  -> #161 odd-bad OR explicit-source-moment-nonzero fork
  -> combine with #159/#163 Riesz, M3/M4 and mixed-source jet structure
  -> test whether simultaneous parity badness survives
  -> if not, test the source-moment alternative for contradiction
```

No theorem currently rules out simultaneous even/odd bad successors. The odd-selected first-bad branch also remains a real coverage gap.

## Pair B — negative-index separation versus localized critical-line sampling rigidity

Pair B remains the secondary independent family. It needs a same-window sampling/frame lower bound strong enough to defeat a sparse exceptional off-line quartet; density percentages alone are insufficient.

# New RH-relevant clues

**LEAD / HYPOTHESIS:** the persistent positive Q14 `J` may be an emergent correlated orientation rather than a quantity decomposable into a simple universally signed set of ingredients. If so, a contradiction may come more naturally from incompatible constraints on the same canonical state than from explaining the sign itself.

**LEAD / HYPOTHESIS:** the #161 parity/source fork may become more informative when treated as a genuine two-branch obstruction rather than as support for an even-only trajectory programme.

Neither lead is a theorem.

# Falsification checks

Before investing in Pair-D formalization:

1. Search for exact canonical or theorem-compatible finite states satisfying current retained-state constraints where both parity successors are bad.
2. Test whether `odd bad OR explicit source moment nonzero` is genuinely restrictive on the target class or whether the nonzero-moment branch is generic.
3. Preserve the same-state firewall: same aperture, same canonical state, same normalization and same production object.
4. Keep the odd-selected branch explicit; there is no WLOG-even reduction.
5. Apply the mustache test: "simultaneous badness is impossible because one parity must be good" is only the desired conclusion restated.
6. Do not use the known #197 `J_POSITIVE` label to select a Pair-D observable after seeing target outputs.

# Highest-leverage next moves

```text
1. synchronize living state through #203
2. preregister a cheap Pair-D exact-state/countermodel falsifier
3. test simultaneous even/odd badness under currently proved same-state constraints
4. inspect the alternative explicit-source-moment branch only if the fork is genuinely restrictive
5. theoremize only a surviving independently meaningful restriction
6. move to Pair B if Pair D remains permissive
```

The next mathematical PR should not be "parity gap v2", another source regrouping, another selector combination, or a larger Q14 numerical budget.

# Standing questions

**Given everything that is now formally true, what becomes possible that was not possible before?**

We can justify changing mathematical families: the planned Pair-A representation programme has now been consumed by a sequence of bounded falsification/nonresolution results while the direct Q14 orientation survives.

**If this contains a clue toward RH, where does that clue propagate?**

Into the same-state parity/source fork and cross-parity Riesz/mixed-jet inventory, not into another equivalent representation of the already-positive Q14 Wronskian.

**What experiment, lemma, reformulation, or connection would most efficiently tell us whether that clue is real?**

A preregistered Pair-D falsifier that asks whether a single canonical retained first-bad state can satisfy both parity-bad branches under the already-proved constraints, before any new large theorem build.

**RH remains OPEN.**
