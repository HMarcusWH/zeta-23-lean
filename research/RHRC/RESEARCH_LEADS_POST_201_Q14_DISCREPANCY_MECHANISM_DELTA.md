# Post-#201 Q14 discrepancy-mechanism research delta

> **Claim firewall: RH remains OPEN.**

This delta records the post-green research consequences of merged PR #201. It does not promote research output into Lean theorem authority.

# What became formally true

No new Lean theorem became formally true in PR #201.

Theorem authority remains PR #184:

```text
validated theorem head = a756494ebe7e2530715e996b9a9a341fbe07c683
merged theorem commit = 6f04e94473eb112b59eeaa2c6fc6ac6cbe7fe30e
validated theorem tree = 6c77cd470809959a403b3bcc5f08d39f4076fa4c
```

The production theorem interface still includes

```text
M~(t)=-tI+R~(t)
P_t' = -envelopeNormSq + remainderEnvelopeDerivative
```

and the conditional negative-orientation theorem under remainder domination.

# What became research-certified

PR #201 is the latest merged research authority:

```text
validated research head = be5b98dfce12777436bc40b39a37b04669ae4311
merged research commit = 319db6f68f65bdcffc0657c03bea76502da59a57
validated research tree = 840a2e8b0bf690507bc3385fbe122210e6d32c7a
```

It reuses the exact frozen Q14 laboratory and first preserves the complete PR #197 bounded result:

```text
post197_classification = GLOBAL_MONOTONE_ORIENTATION
post197_uniform_orientation = J_POSITIVE
completed_leaf_count = 49
```

It also preserves the post-#199 diagnosis:

```text
post199_mechanism_classification = SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED
```

The new representation is built **before** parity restriction and interval transport:

```text
D = pole + prime_signed
A = direct_arch_signed
M = D + A
```

with matrix jets

```text
D,D',D''
A,A',A''
```

and only three normalized Wronskian interaction groups:

```text
J_DD
J_DA
J_AA
```

Paired `M,M',M''` reconstruction and the derivative implementation checks pass.

The final bounded mechanism output is:

```text
paired_source_sum_positive_leaf_count = 0
paired_source_sum_unresolved_leaf_count = 49
mechanism_classification = DISCREPANCY_REPRESENTATION_DEPENDENCY_UNRESOLVED
selected_uniform_lock_group = null
control_transfer_status = NO_UNIQUE_PRIMARY_LOCK
theorem_promotion = false
fb05_closed = false
negative_root_exclusion = false
rh_claim = false
```

# What changed

The post-#199 hypothesis was concrete: perhaps the source-mechanism audit lost the already-known positive sign only because pole and prime were independently enclosed before their theorem-motivated cancellation was formed.

PR #201 tests that hypothesis directly. Pole and `prime_signed` are paired at matrix-jet level first. The resulting representation is implementation-valid and reconstructs the same canonical matrix jets, yet it still fails to certify the positive paired source sum on every completed leaf.

Therefore the route has learned something stronger than “no single source channel dominates.”

The dependency loss is not repaired merely by moving the known pole-prime cancellation upstream.

This does **not** say that `J` became unsigned. The independent direct Method-C result remains `J_POSITIVE` on the exact completed #197 hull.

# Upstream implications

The main upstream consequence is methodological:

```text
four-way source split                         -> dependency unresolved
collapsed pole/direct-arch/prime split       -> dependency unresolved
upstream pole-prime discrepancy/direct-arch  -> dependency unresolved
```

Arbitrary further linear regrouping of source channels should therefore not be treated as a new route unless an independent theorem specifically selects the grouping.

DR-020 is strengthened rather than revived: exact cancellation must not be destroyed, but preserving one theorem-motivated cancellation is still insufficient for the present attribution graph.

The exact pole-prime discrepancy theorems remain useful interfaces. PR #201 does not falsify them; it falsifies their sufficiency as the current parity-orientation mechanism extractor.

# Downstream implications

The strongest surviving bounded fact is still the assembled trajectory result:

```text
GLOBAL_MONOTONE_ORIENTATION
uniform J_POSITIVE
```

on the exact frozen Q14 domain.

The next mathematical question is no longer “which source bucket is positive?” It is:

> What exact relation of the fully assembled canonical parity object could explain or generalize the direct orientation without destroying its correlations?

Equivalent formulas such as

```text
(o/e)' = J/e^2
(log(o/e))' = J/(oe)
```

remain legal DERIVED identities, but they do not provide a new reason for `J>0`.

# Resurrected routes

The strongest surviving Pair-A route is a full-composite parity formulation.

Use normalized predecessor levels

```text
E = e / ||W_e||^2
O = o / ||W_o||^2
G = O - E
```

with the exact algebraic factorization

```text
Jbar = O' E - E' O = E G' - E' G.
```

The factorization is not itself a new result. Its research value would come from a simpler independent sign structure in `E`, `E'`, `G`, `G'`, `E*G'`, or `-E'*G`.

If that route fails, the genuinely different same-state alternatives Pair D and Pair B should rise in priority.

# New RH-relevant clues

## LEAD — fully assembled parity correlation may be the native object

The persistence pattern is suggestive:

```text
direct correlated J             -> rigorously positive on the full frozen hull
separated source attribution     -> unresolved
paired discrepancy attribution   -> unresolved
```

A plausible explanation is that the useful sign is emergent at the level of the assembled parity object rather than attached to a small number of separately transported source contributions.

This is a LEAD / HYPOTHESIS, not a theorem.

## LEAD — common scalar drift cancellation

Because PR #184 supplies the production/log-cover structure `M~(t)=-tI+R~(t)`, normalized even/odd predecessor differences may cancel the common scalar drift before any source decomposition. That makes parity-gap quantities worth falsifying directly.

Again, this is a lead, not an established sign law.

# Falsification checks

The next Pair-A experiment must be cheap to kill.

Predeclare a simple center-level candidate pattern, for example

```text
E > 0
G > 0
G' >= 0
E' <= 0
```

and the component terms

```text
T1 = E*G'
T2 = -E'*G.
```

Then:

1. evaluate the six inherited exact Q14 primary centers first;
2. if the frozen pattern fails at any center, classify the mechanism as falsified and stop;
3. do not fit a replacement pattern from the known `J` label;
4. if it survives, reuse the exact #197 49-leaf partition at 384 bits;
5. do not increase Q/N/K/parity, precision, depth or cell budget;
6. require the gap factorization to reconstruct independent direct Method C;
7. run the mustache test: an equivalent restatement of `J>0` is not a new constraint;
8. **Do not count them as two independent** when two formulations encode the same signed quantity.

# Highest-leverage next moves

```text
1. synchronize the living SSOT/control state through #201
2. run one full-composite parity-gap falsifier with an exact-center kill-switch
3. if a clean independent component law survives, isolate the smallest generic theorem
4. theoremize only the generic algebra/monotonicity bridge first
5. separately attack the canonical arithmetic premise on arbitrary retained first-bad states
6. if the composite mechanism is mixed or dependency-unresolved, stop Pair-A representation engineering
7. prioritize Pair D, then Pair B
```

# Standing questions

Given everything that is now formally/research-certifiably true, what becomes possible that was not possible before?

> We can stop treating simple source regrouping as the default explanation for the bounded Q14 orientation. That lane has now been tested both independently and with theorem-motivated upstream pole-prime cancellation.

If this contains a clue toward RH, where does that clue propagate upstream or downstream through the existing mathematics?

> Upstream toward the fully assembled parity/contact structure; downstream toward a same-state incompatibility that composes a genuine composite parity restriction with #184 and an independent first-bad property.

What experiment, lemma, reformulation, or connection would most efficiently tell us whether that clue is real?

> A predeclared full-composite parity-gap sign test on the six frozen centers, followed only if it survives by the unchanged 49-leaf interval cover.

**RH remains OPEN.**