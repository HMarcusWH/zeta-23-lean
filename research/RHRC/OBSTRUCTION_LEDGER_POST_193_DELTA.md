# RHRC obstruction ledger — post-#193 delta

> **Claim firewall: RH remains OPEN.**
>
> This supplement records reusable obstruction information learned from PR #193. It does not replace the historical post-#190 obstruction delta and does not promote research evidence into theorem authority.

## OBS-055 — first-order centered parity-trajectory enclosure is dependency-limited on the frozen Q14 hull

**Status:** RIGOROUS FINITE NONRESOLUTION / REPRESENTATION OBSTRUCTION  
**Scope:** inherited fixed-Q Q14 primary hull used by PR #193  
**Does not mean:** trajectory monotonicity failed

### Exact-center record

All six inherited exact primary-center Arb evaluations certify:

```text
6/6  e  positive
6/6  o  positive
6/6  J  positive
6/6  P2 positive
```

where

```text
J = o'e - e'o
P2 = L*J/(o*e).
```

### First-order finite-width cover

The frozen #193 cover evaluates 96 cells:

```text
63 H1_UNRESOLVED
33 J_UNRESOLVED after H1 recovery
0 J_NEGATIVE
0 J_POSITIVE
full hull unresolved
classification = TRAJECTORY_RIGIDITY_UNRESOLVED
```

No finite-width orientation sign is certified anywhere by the declared first-order graph.

### Obstruction statement

The #193 first-order centered dependency graph is insufficient, under its frozen cell/depth budget, to certify finite-width parity-trajectory orientation on the inherited Q14 hull.

The failure has two explicit layers:

```text
first gate:
  centered H1 cannot be recovered on 63/96 evaluated cells

second gate:
  on the remaining 33/96 cells H1 is recovered,
  but J=o'e-e'o still wraps zero
```

This is reusable dead-route information because it says that merely spending more PRs on the same representation with only a larger routine depth/precision budget is not yet a new mathematical mechanism.

### What OBS-055 does not establish

OBS-055 does **not** establish:

```text
J has a zero on the hull
J changes sign on the hull
there is a trajectory fold
P1 is non-monotone
a reflected twin exists
a reflected twin does not exist
FB-05 is closed
negative-root exclusion
RH
```

In particular:

> first-order nonresolution does not falsify monotonicity.

The exact-center evidence instead motivates testing whether a dependency-reduced representation can propagate the uniformly positive point orientation.

### Required route change

The next experiment must change the representation while preserving the population:

```text
same frozen Q14 hull
same six inherited centers
same Q/N/K/parity
same inherited P1/P2/J graph
no target labels
```

Compare against the #193 baseline:

```text
A. first-order Wronskian enclosure
B. direct P2 log-slope enclosure
C. centered second-order/Taylor enclosure
```

A complete signed cover is required before reporting bounded monotonicity.

### Historical relationship

OBS-054 remains untouched and continues to record that frozen strong-selector composition is insufficient in the #190 ambient normalized algebra. OBS-055 is different:

```text
OBS-054
  observation-surface insufficiency in ambient algebra

OBS-055
  neighborhood-enclosure insufficiency on the canonical Q14 trajectory
```

The two obstructions compose into the current routing picture:

```text
ambient selector vector insufficient
  -> impose canonical production structure
  -> specific #190 witness excluded, general mechanism unresolved
  -> move to actual aperture trajectory
  -> exact-center orientation uniformly positive
  -> first-order finite-width propagation still dependency-limited
  -> sharpen representation on the same hull
```

**RH remains OPEN.**
