# Obstruction ledger delta after PR #235

> **RH remains OPEN.**

## What became formally true

PR #235 proves the exact retained source-kernel forbidden-quadrant interface on
the selected-even / odd-good branch:

```text
D <= 0 OR E <= 0,
```

hence `D>0 AND E>0` forces odd badness.

## Workflow harvest

All 11 attached #235 workflows completed green. Formal compiler/axiom gates
validated the theorem family. Research replays remained regression/research
evidence and were not promoted to theorem authority.

## What changed

Consumed target:

```text
RETAINED_SOURCE_KERNEL_FORBIDDEN_QUADRANT = PROVED_PR_235
```

New target:

```text
RETAINED_CANONICAL_REAL_PHASE_COLLAPSE
```

Required structural input:

```text
CONJUGATION_COMPATIBLE_CANONICAL_NORMALIZATION
```

OBS-059I remains OPEN.

## Upstream implications

The real-entry canonical matrix and canonical cubic normalization can now be
composed. This prerequisite was unavailable before the normalized E3-A
eigenmode theorem.

## Downstream implications

A successful phase collapse gives one real source scalar `s` with

```text
normSq(S)=s^2,
D <= 0 OR D^2 <= s^2 Rsharp.
```

That would remove one real degree of freedom from the retained odd-good
obstruction.

## Resurrected routes

Real-eigenvector reasoning is resurrected only in its normalized form. Bare
Hermitianity remains insufficient.

## New RH-relevant clues

**LEAD / HYPOTHESIS:** the retained selected-even state is fixed by
coordinatewise conjugation because its canonical quotient coordinate is one.

## Falsification checks

Do not assume eigenvalue simplicity.

Do not identify Hermitianity with real-entry structure.

Do not infer a sign for the resulting real source scalar.

Do not upgrade one-real-variable compression to retained-state exclusion.

## Highest-leverage next moves

Formalize conjugation-compatible canonical normalization and then rewrite the
#235 compatibility law with one real source scalar.

## Standing questions

Can the exact retained trial be proved conjugation-fixed without new spectral
assumptions?

After the phase collapse, what canonical arithmetic controls the center deficit
and sharp radius gap?

Can that control force the selected-even branch into simultaneous odd badness?
