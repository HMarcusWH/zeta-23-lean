# RHRC obstruction-ledger delta after PR #190

> **Claim firewall: RH remains OPEN.**
>
> This delta supplements the historical obstruction ledger. It does not rewrite earlier deltas or promote research evidence to theorem authority.

## Authority

```text
formal theorem authority = PR #184
validated theorem head = a756494ebe7e2530715e996b9a9a341fbe07c683
merged theorem commit = 6f04e94473eb112b59eeaa2c6fc6ac6cbe7fe30e
theorem tree = 6c77cd470809959a403b3bcc5f08d39f4076fa4c

latest research evidence = PR #190
validated research head = 8701920b0da18ae6595ad0eee55c1f6cb291a94f
merged research commit = f87da9fde71dd1e74419c6ae5848eee3787c27e4
research tree = af8774b65c898de221a5bf32977ccff3407a7b2d
research disposition = JOINT_EXACT_VECTOR_SEPARABLE / JOINT_THRESHOLD_SIGNATURE_SEPARABLE
```

## OBS-054 — frozen strong-selector composition is insufficient in the ambient normalized algebra

**Status:** EXACT EXECUTABLE FALSIFICATION OF SUFFICIENCY / CANONICAL REALIZABILITY REMAINS OPEN.

PR #188 froze the strong-eligible observation surface

```text
G1_COUPLING_RATIO
P1_PARITY_LEVEL_RATIO
P2_PARITY_LOG_SLOPE_GAP
C1_PRIME_PIVOT_CONTRIBUTION
C2_ARCH_PIVOT_CONTRIBUTION
C3_POLE_PIVOT_CONTRIBUTION
C4_PRIME_VS_SMOOTH_MAGNITUDE
```

PR #189 established exact coordinate-by-coordinate semantic separability in the normalized ambient algebra.

PR #190 closes the remaining composition hole. It certifies an exact rational pair satisfying the audited reconstruction identities with:

```text
same complete seven-dimensional strong vector
same nonboundary threshold signature
opposite normalized FB-05 target signs
```

The exact classifications are:

```text
JOINT_EXACT_VECTOR_SEPARABLE
JOINT_THRESHOLD_SIGNATURE_SEPARABLE
```

Because the full vector agrees, all `2^7 - 1 = 127` nonempty subsets also agree across the same target-flipping pair.

### Consequence

Do not target a universal FB-05 discriminator that is merely a deterministic function of the frozen seven-observable surface inside the same ambient algebra. This includes:

```text
Boolean combinations of the seven frozen threshold relations
searches over their 127 subsets
threshold refitting over the same coordinates
new scalar formulas using only the same seven exact values
```

The information is absent from the full vector itself.

### What this obstruction does not establish

The #190 pair is not proved to be the image of actual canonical arithmetic production. **Canonical arithmetic realizability remains open; canonical arithmetic realizability is not established by #190.** Therefore OBS-054 does **not** imply:

```text
canonical arithmetic states admit opposite-target twins
Pair A is dead
no additional production invariant can close FB-05
negative-root exclusion fails
RH is false or true
```

### New live obstruction boundary

The remaining question is whether the canonical production image is a sufficiently small subset of the ambient normalized state space to eliminate the reflected branch.

The existing executable source construction already shows one omitted coupling:

```text
scalar_shift = 2*cCorrection'(L) I
arch_signed  = -arch_direct - scalar_shift.
```

This is a candidate first violated realizability condition, not yet a theorem that excludes the whole reflected class.

### Required next test

Progressively impose:

```text
source-channel construction identity
exact scalar-aperture law
common direct-arch derivative
common Schur geometry
full same-L/same-Q/same-parity canonical source reconstruction
```

and identify the first layer at which an opposite-target twin is exactly impossible. If bounded computation merely finds no twin, classify `UNRESOLVED`.

### Permanent warning

#190 closes **ambient selector sufficiency**, not **canonical production realizability**. Future documentation and routing must preserve that distinction.

**No promotion:** FB-05, negative-root exclusion and RH remain OPEN.