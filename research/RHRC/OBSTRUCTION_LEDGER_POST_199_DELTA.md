# Obstruction ledger supplement after PR #199

> **RH remains OPEN.**

This supplement preserves the earlier obstruction ancestry while recording the source-decomposition result from PR #199.

## OBS-056 — residual one-span canonical trajectory orientation dependency

**Status:** CLOSED IN THE EXACT FROZEN Q14 RESEARCH SCOPE by PR #197.

The completed inherited cover remains:

```text
GLOBAL_MONOTONE_ORIENTATION
uniform_orientation = J_POSITIVE
unresolved_span_count = 0
certified_t_fraction = 1
full_hull_signed_monotonicity = true
global_positive_hull = true
bounded_distinct_aperture_twin_exclusion = true
```

PR #199 replays this exact state before mechanism analysis. Nothing in #199 reopens OBS-056.

## OBS-057 — bounded-to-structural parity-ordering mechanism gap

**Status:** OPEN / NARROWED.

The statement after PR #197 was that frozen Q14 has a complete positive orientation certificate but no theorem-backed canonical arithmetic relation explains why the sign persists or transports it to the arbitrary retained first-bad/contact state.

PR #199 narrows that obstruction:

```text
four_way_classification = SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED
collapsed_three_way_classification = SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED
mechanism_classification = SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED
```

Thus the first explicit pole/arch/prime source-attribution graph does not provide the missing structural law. The direct bounded `J_POSITIVE` result remains intact.

OBS-057 would still be materially narrowed or closed by:

1. a compact exact canonical source relation yielding parity ordering on a class containing the retained first-bad state;
2. a theorem-backed cross-source relation whose hypotheses are independently available on that state;
3. a rigorous falsification showing the bounded Q14 orientation is nontransferable for a structural reason rather than merely representation-limited.

## OBS-058 — cancellation-preserving source representation gap

**Status:** OPEN / ACTIVE RESEARCH OBSTRUCTION.

Statement:

> On the exact frozen Q14 trajectory, the independently evaluated direct Method-C parity Wronskian remains rigorously positive, but both the four-way and collapsed three-way source-interaction interval representations lose enough dependency information that they cannot certify the same sign after decomposition.

This is a representation/correlation obstruction:

```text
direct correlated J transport = signed positive
source-separated J transport = dependency unresolved
```

It does **not** imply:

```text
J is actually sign-indefinite
no source mechanism exists
pole-prime discrepancy fails
FB-05 is false
negative-root exclusion is impossible
RH is false or true
```

### Why this is distinct from DR-020

DR-020 is the reusable warning against loose independent source-channel majorants that discard exact cancellation. OBS-058 is the current live manifestation of that dependency problem in the parity-Wronskian mechanism audit.

No new dead-route ID is required.

### What would close or narrow OBS-058

Any one of the following would materially narrow it:

1. a cancellation-preserving paired source representation that reconstructs the direct `J` sign over the exact inherited cover;
2. an exact algebraic factorization of `J` or `J'` reducing dependency before interval enclosure;
3. a theorem-level relation bypassing source decomposition entirely;
4. rigorous falsification showing that even cancellation-preserving representations fail for a structural reason rather than interval inflation.

### Preferred next diagnostic

Use the theorem-motivated pole-prime discrepancy before interval transport:

```text
D = pole + prime_signed
A = direct_arch_signed
M = D + A
```

Construct `D,D',D''` and `A,A',A''` first, then parity-restrict and form only

```text
J_DD
J_DA
J_AA.
```

Require

```text
J_DD + J_DA + J_AA
```

to rigorously reconstruct the independent direct Method-C enclosure on every inherited #197 leaf.

If this remains dependency-unresolved, do not add more source subchannels or merely increase precision/depth. The next move should be a higher-level composite identity or a different same-state incompatibility pair.

## Historical relation

- `OBS-054` remains the ambient-selector insufficiency obstruction from PR #190.
- `OBS-055` remains the first-order finite-width representation failure from PR #193.
- `OBS-056` records the post-#195 residual orientation span and is closed in its declared scope by PR #197.
- `OBS-057` remains the bounded-to-structural mechanism gap and is narrowed by PR #199.
- `OBS-058` records the new cancellation-preserving source representation gap.

## Claim firewall

```text
#197 GLOBAL_MONOTONE_ORIENTATION
  -> rigorous bounded research

#199 SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED
  -> current decomposition cannot expose the signed direct quantity
  -/-> direct sign lost
  -/-> arbitrary-Q theorem
  -/-> global canonical injectivity
  -/-> same-state FB-05 contradiction
  -/-> negative-root exclusion
  -/-> RH
```

Theorem authority remains PR #184. **RH remains OPEN.**