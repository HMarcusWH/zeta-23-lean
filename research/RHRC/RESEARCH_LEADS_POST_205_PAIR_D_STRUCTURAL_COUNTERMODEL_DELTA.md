# Post-#205 Pair-D structural-countermodel research delta

> **Claim firewall: RH remains OPEN.**
>
> This document records the post-green research consequences of PR #205. It is research-routing evidence, not Lean theorem authority.

# What became formally true

No new Lean theorem became true in PR #205. The compiler-validated theorem authority remains PR #184.

The formal first break remains `E4A4-SCHUR-FB-05`; R003 remains `DISCOVERY`; confirmatory execution is not authorized.

# What became research-certified

PR #205 upgrades historical fixture C1 to current exact executable research on the repository's actual radius-3 parity/boundary-flat/shell geometry:

```text
validated research head = 73b78297da54b9f5b2d47584a8033356a6b2e2a8
merged research commit = deaa69ae190ada511cf8228f174846184673ff3a
validated research tree = 3d715612eabf25a9056ab84b0c5e968f71354666
classification = PAIR_D_GENERIC_SIMULTANEOUS_BAD_COUNTERMODEL_CERTIFIED
fixture = C1
N = 2
K = 3
q_by_abs_index = (1,1,1,-10)
even predecessor form = 70 > 0
odd predecessor form = 10 > 0
even successor witness energy = -130
odd successor witness energy = -410
selected even compressed root = -13/42
reversal_symmetric = true
centered_index_commutator_zero = true
canonical_realizability = false
```

Evidence class:

```text
EXACT EXECUTABLE RESEARCH
RIGOROUS FINITE SYNTHETIC COUNTERMODEL
```

# What changed

Before #205, the Pair-D question was whether simultaneous even/odd successor badness might be excluded by the already-proved generic predecessor/parity/shell/displacement structure.

After #205, that generic lane is consumed:

```text
predecessor positivity
+ actual radius-3 parity/boundary-flat/shell geometry
+ reversal symmetry
+ centered-index displacement commutation
  -/-> simultaneous-parity exclusion
```

This does **not** show simultaneous badness for the actual `canonicalSourceMatrix`. Instead it isolates the missing information more sharply: any successful Pair-D exclusion must spend a property specific to canonical arithmetic that C1 does not possess.

# Upstream implications

The following structures are insufficient on their own for the desired terminal exclusion:

- predecessor positivity in both parity sectors;
- one-step parity/shell geometry;
- reversal symmetry;
- centered-index displacement commutation;
- generic Hermitian/KKT refinements that remain true of C1.

Therefore future theorem design should ask which exact pole/archimedean/prime property excludes C1, rather than adding another generic parity lemma.

PR #205 strengthens the existing DR-012 and DR-013 firewalls. It does not create a new dead-route family.

# Downstream implications

The exact #161 retained-state fork remains central:

```text
opposite parity bad
OR
explicitCanonicalSourceMoment != 0
```

PR #205 shows that the first branch cannot be removed by generic parity/shell/displacement geometry alone.

The remaining Pair-D obligations are therefore:

```text
canonical simultaneous even/odd bad exclusion      OPEN
canonical sourceMoment <-> M4 rigidity              OPEN
same-state canonical arithmetic composition         OPEN
odd-selected first-bad closure                      OPEN
```

Pair B — negative-index separation versus localized critical-line sampling rigidity — remains secondary and independent.

# Resurrected routes

The source-moment/M4 seam gains relative importance because the easy generic simultaneous-bad exclusion has now been defeated exactly.

The useful next question is not yet a canonical theorem. It is a cheap structural falsifier:

```text
genericQuadraticNormalPairing != 0
  ?-> M4 != 0
```

The synthetic observable must be named neutrally. `explicitCanonicalSourceMoment` remains reserved for the actual canonical pole/archimedean/prime formula.

# New RH-relevant clues

**LEAD / HYPOTHESIS:** if both the generic simultaneous-bad exclusion and the generic defect-to-`M4` bridge fail exact structural countermodels, then FB-05 is forced onto genuinely canonical arithmetic coefficients rather than generic finite-dimensional geometry.

That would not prove RH. It would compress the remaining theorem search to the arithmetic image of `canonicalSourceMatrix`.

# Falsification checks

Before theoremizing any sourceMoment/`M4` bridge:

1. use exact rational arithmetic on the same radius-3 structural geometry;
2. ask whether a generic quadratic-normal pairing can be nonzero while `M4=0`;
3. freeze the candidate fixture before treating the output as a route result;
4. do not call the synthetic pairing `explicitCanonicalSourceMoment`;
5. do not infer canonical realizability from a synthetic example;
6. if a generic countermodel exists, consume only the generic implication;
7. preserve the odd-selected branch as an open coverage obligation.

A theorem proposal that merely assumes successor goodness, absence of simultaneous badness, or canonical positivity under another name fails the mustache test.

# Highest-leverage next moves

```text
1. synchronize living documentation/control state through #205;
2. run one exact generic defect-versus-M4 falsifier;
3. if the generic bridge fails, isolate the canonical pole/archimedean/prime relation that excludes the countermodel;
4. theoremize only a surviving independently meaningful canonical relation;
5. compose it on the exact retained first-bad state;
6. keep Pair B as the next independent family if Pair D remains weak.
```

Do not return to Pair-A representation refits, larger Q14 budgets, or another generic simultaneous-parity exclusion.

# Standing questions

Given everything now formally true and research-certified, what becomes possible that was not possible before?

> We can now reject generic simultaneous-parity exclusion as a terminal Pair-D mechanism and require any future exclusion theorem to identify the canonical arithmetic premise that C1 violates.

If this contains a clue toward RH, where does that clue propagate?

> It propagates through the #161 parity/source fork and the #159/#163 Riesz/mixed-source inventory, narrowing attention to canonical source arithmetic and the sourceMoment/`M4` interface.

What experiment or lemma most efficiently tests whether the clue is real?

> An exact generic `genericQuadraticNormalPairing != 0 ?-> M4 != 0` falsifier, followed only then by a canonical theorem attempt if the structural bridge survives.

Theorem authority remains PR #184. Negative-root exclusion remains OPEN. **RH remains OPEN.**