# Post-#213 complete source-kernel delta

> **Claim firewall: RH remains OPEN.**

PR #213 is the current theorem-bearing authority. Validated theorem head `703c3764a7d35aa4801e791a1929efa54c2533a1`; merged theorem commit `ee341a6071d177c75bbea0a5f92ebe3b3bb16696`; merged tree `db00686b2bbb821adb857e5c68f422d19c4f91cd`. Latest research-only evidence remains PR #205. Control semantic authority remains PR #117.

# What became formally true

**PROVED.** Zero-centered mixed finite dictionary tests have an exact complete physical RHS:

```text
half_literatureRHS_dictionaryMixedTest_eq_completePhysicalRHS_of_zero
```

**PROVED.** For the active source observable
[
h_v(omega)=quadraticNormalSourceAtom(K,v,omega),
]
the complete functional is exactly the source-coordinate kernel
```text
canonicalQuadraticNormalSourceFunctional_eq_sourceKernelRHS
explicitCanonicalSourceMoment_eq_sourceKernelRHS
```

with
[
mathcal K_{L,K}(v)
=
int_0^L h_v(1-x/L)(e^{-x/2}+e^{x/2}),dx
-int_0^L h_v(1-x/L)ho(x),dx
-sum_{qle e^L}rac{Lambda(q)}{sqrt q}
h_v(1-log q/L).
]

**PROVED.** On the retained even-selected state, merged-green #209 can therefore be rewritten as
```text
evenShiftedSourceKernelMixedJet_re_neg_of_even_of_not_oddBad
oddBad_or_sourceKernelMixedJet_re_neg_of_even
```
that is,
[
ParityBad_{odd}
quadlorquad
Re(overline{mathcal K_{L,K}(v)},h_v^{(7)}(0))<0.
]

This is still a representation/interface rewrite of the #209 anti-alignment. It is not independent incompatibility information.

# What changed

The representation problem is finished.

The active chain is now
```text
#209  quantitative coercivity / compulsory anti-alignment
  ↓
#211  source moment = complete explicit-formula functional Λ_L(h_v)
  ↓
#213  Λ_L(h_v) = exact source-coordinate continuous-minus-prime kernel
```

`SOURCE_COORDINATE_KERNEL_FOR_COMPLETE_FUNCTIONAL` is therefore CLOSED / PROVED BY #213.

`OBS-059I` remains OPEN / ACTIVE because the project still lacks genuinely independent structure incompatible with the #209 anti-alignment or magnitude budget.

# Upstream implications

**DERIVED / NOT YET PACKAGED AS A LINEARMAP THEOREM.** For fixed (L,K), the maps
[
vmapsto h_v(omega),qquad
vmapstomathcal K_{L,K}(v),qquad
vmapsto h_v^{(7)}(0)
]
inherit linear dependence on the even-boundary-flat vector (v) from the existing matrix-moment, source-atom and seventh-jet definitions.

This suggests a small future abstraction:
```text
kernel functional F_{L,K} : V_K →ₗ[ℂ] ℂ
seventh-jet functional G_K : V_K →ₗ[ℂ] ℂ
```
but those LinearMap packages are not yet theoremized.

# Downstream implications

The next research operation is

```text
EXACT_KERNEL_ADVERSARIAL_FALSIFICATION
```

on the exact #213 functional.

Three preregistered lanes:

```text
SIGN
MAGNITUDE
DUAL / GRAM / EXTREMAL GEOMETRY
```

The goal is to learn whether any genuinely independent law survives before another large theoremization.

# Resurrected routes

**Riesz route: relevant but not directly composable.**

Existing `CanonicalPolePrimeDiscrepancy` / `CanonicalPolePrimeRiesz` machinery acts on `sourceAtomRealEnergy`. PR #213 acts on the complex mixed `quadraticNormalSourceAtom`. The old Riesz theorems are therefore not automatically the same-observable bridge required here.

A future mixed-observable Riesz analogue is a legitimate lead, but it is OPEN.

**Component sign route remains consumed.**

PRs #199/#201 remain warnings that exposing pole, archimedean and prime pieces does not license independent sign claims for them.

# New RH-relevant clues

**LEAD / HYPOTHESIS — dual-functional geometry.**

Let
[
F_{L,K}(v)=mathcal K_{L,K}(v)
]
and
[
G_K(v)=h_v^{(7)}(0).
]

Then the forced #209 quantity becomes
[
Q(v)=Re(overline{F_{L,K}(v)},G_K(v)).
]

If (F) and (G) are packaged as linear functionals, (Q) is a Hermitian quadratic form generated by two covectors and hence has very low-rank structure. This could turn the incompatibility problem into a concrete finite-dimensional geometry question.

Immediate falsifier questions:
- are (F) and (G) proportional?
- what is the rank of their joint map?
- what is their Gram matrix / angle?
- what signs can (Q(v)) take on the full even-boundary-flat space?
- what changes after restricting to retained eigenspaces?
- what are the extremal values of (-Q(v)/|Dv|^2)?

# Falsification checks

- A sign law on the full even-boundary-flat space may fail while a sign law on the retained negative-eigenmode locus survives.
- If the retained eigenspace is one-dimensional, proportionality after restriction is automatic and the scalar ratio becomes the real object.
- A dual-norm bound is insufficient unless it interacts with the retained eigenvalue equation and the #209 energy budget.
- Do not infer a mixed-observable Riesz theorem from the real-energy Riesz machinery.
- Do not split the complete kernel into independently signed pole/arch/prime pieces without a theorem that preserves the required dependence.
- The simultaneous odd-bad branch remains OPEN.
- The odd-selected first-bad branch remains OPEN.
- Endpoint-scalar sign remains OPEN.
- Negative-root exclusion remains OPEN.
- **RH remains OPEN.**

# Highest-leverage next moves

1. Synchronize living docs/control through #213.
2. Build an exact research evaluator for (F_{L,K}) and (G_K) on the canonical even-boundary-flat space.
3. Test proportionality, joint rank, Gram geometry and sign range before any large theorem proof.
4. Test the energy-normalized quantity (-Re(overline{F(v)}G(v))/|Dv|^2).
5. Restrict progressively to exact retained-state constraints only after the full-space falsifier is understood.
6. Formalize only a property that survives those adversarial checks.

# Standing questions

What became possible after #213?

The complete Pair-D functional is no longer opaque: it is an exact continuous-plus-prime source-coordinate functional of the same observable whose seventh local jet appears in #209.

Where can an RH-relevant clue propagate?

Into the finite-dimensional dual geometry of the two exact functionals, or into a genuinely same-observable mixed Riesz/extremal theorem.

What is the highest-information next test?

Determine whether the kernel functional and seventh-jet functional are proportional or otherwise geometrically constrained on the relevant canonical spaces, and falsify universal SIGN/MAGNITUDE candidates before theoremization.

The selected formal first break remains `E4A4-SCHUR-FB-05`. R003 remains `DISCOVERY`; confirmatory execution is not authorized. **RH remains OPEN.**
