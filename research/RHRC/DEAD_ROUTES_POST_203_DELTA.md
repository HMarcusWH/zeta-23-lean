# RHRC dead-route delta after PR #203

> **Claim firewall: RH remains OPEN.**

This dated delta extends `DEAD_ROUTES.md` without rewriting the historical monolithic ledger. Earlier DR entries remain authoritative for their own scope.

## DR-020 — post-#203 clarification

DR-020 already quarantines independent coarse channel majorants and records the #199/#201 evidence that source-separated and theorem-motivated paired representations lose the sign while direct Method C remains `J_POSITIVE`.

PR #203 consumes the first preregistered higher-level composite escape from that source-attribution lane. It preserves the assembled parity object, removes the common scalar direction before interval transport, independently reconstructs the normalized parity gap, and tests the frozen conjunction

```text
E > 0
G > 0
G' >= 0
E' <= 0
```

The conjunction fails its six-center gate:

```text
COMPOSITE_PARITY_GAP_PATTERN_FALSIFIED
center_kill_switch_survives = false
full_cover_executed = false
completed_leaf_count = 0
PATTERN_FALSIFIED_BEFORE_FULL_COVER
```

Therefore the default escape from DR-020 is no longer "try another nearby composite representation of the same Q14 Wronskian." The live route should change mathematical families unless new theorem content independently selects a representation.

This clarification does **not** make all exact correlated identities dead. It raises the revival bar: a new Pair-A representation needs independent theorem motivation rather than post-hoc target fitting.

## DR-028 — post-hoc rescue of the falsified composite parity-gap sign pattern

**Attempt:** After observing PR #203, search nearby sign combinations, subsets of the six centers, fitted thresholds, or equivalent ratio/derivative formulations until the full-composite parity-gap mechanism appears to sign the known Q14 orientation.

**Status:** **DEAD AS A DISCOVERY STRATEGY / exact preregistered center-level falsification in PR #203.**

The frozen candidate was

```text
E > 0
G > 0
G' >= 0
E' <= 0
```

and it was tested before any 49-leaf full-cover mechanism run. The green certifier returned

```text
COMPOSITE_PARITY_GAP_PATTERN_FALSIFIED
center_kill_switch_survives = false
full_cover_executed = false
completed_leaf_count = 0
```

The correct scientific response is to keep the falsification, not to mutate the candidate after unblinding.

Therefore the following do **not** count as a route change:

```text
flip one primitive sign after seeing the centers
choose only the centers that fit
fit a threshold to G or G'
replace the conjunction with an equivalent J/ratio identity
increase Q14 precision/depth/cells to rescue a center-level failure
return to source-parenthesis changes without new theorem content
```

**Nonclaim:** DR-028 does not prove every possible Pair-A theorem false. It kills only post-hoc rescue of the tested representation family as a discovery strategy.

**Revival requirement:** state an independently derived theorem-level invariant before target inspection, show why it is mathematically distinct from the failed conjunction and from known `J>0`, and then preregister the relevant falsification surface.

## Routing consequence

```text
Pair-A representation engineering = CONSUMED / DOWNGRADED
Pair D same-state two-parity squeeze = PRIMARY
Pair B negative-index vs localized sampling rigidity = SECONDARY
```

The direct PR #197 `GLOBAL_MONOTONE_ORIENTATION / J_POSITIVE` result remains rigorous bounded research and is not weakened by DR-028.

Negative-root exclusion remains OPEN. **RH remains OPEN.**
