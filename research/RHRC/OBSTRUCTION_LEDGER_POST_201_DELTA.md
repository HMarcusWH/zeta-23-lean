# RHRC obstruction ledger delta after PR #201

> **Claim firewall: RH remains OPEN.**

This delta narrows the living FB-05 obstruction picture after merged-green PR #201. It does not promote a theorem or close FB-05.

## OBS-056 — residual frozen-Q14 orientation span

**Status:** CLOSED in the exact frozen Q14 research scope by PR #197.

Preserved bounded result:

```text
GLOBAL_MONOTONE_ORIENTATION
uniform orientation = J_POSITIVE
completed_unresolved_span_count = 0
certified_t_fraction = 1
global_positive_hull = true
bounded_distinct_aperture_twin_exclusion = true
```

This remains bounded research, not arbitrary-Q theorem authority.

## OBS-057 — bounded-to-structural parity-ordering mechanism gap

**Status:** OPEN / FURTHER NARROWED.

Historical post-#199 status was `OBS-057 OPEN / NARROWED`.

What is now known:

- PR #197 rigorously signs the complete frozen Q14 trajectory;
- PR #199 fails to extract the sign through independently transported four-way or collapsed-three-way source interactions;
- PR #201 also fails to extract the sign after forming the theorem-motivated pole-prime discrepancy upstream before parity restriction and interval transport.

So the remaining gap is narrower:

> find an independently meaningful canonical **composite** restriction that explains/generalizes the bounded parity ordering and can plausibly apply to an arbitrary retained first-bad/contact state.

Equivalent rewrites of `J>0` do not close this obstruction.

## OBS-058 — cancellation-preserving source representation gap

**Status:** OPEN / NARROWED THROUGH #201.

PR #199 established:

```text
SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED
```

for both the four-way and collapsed three-way source representations while preserving direct `J_POSITIVE`.

PR #201 executes the preferred cancellation-preserving diagnostic:

```text
D = pole + prime_signed
A = direct_arch_signed
M = D + A
```

with `D,D',D''` and `A,A',A''` constructed before parity restriction and interval transport.

Implementation/reconstruction checks pass, but the completed frozen cover gives:

```text
paired_source_sum_positive_leaf_count = 0
paired_source_sum_unresolved_leaf_count = 49
mechanism_classification = DISCREPANCY_REPRESENTATION_DEPENDENCY_UNRESOLVED
selected_uniform_lock_group = null
control_transfer_status = NO_UNIQUE_PRIMARY_LOCK
```

Interpretation:

```text
upstream pole-prime pairing is mathematically/implementation legitimate
+
direct correlated Method C remains J_POSITIVE
+
paired attribution still loses sign resolution

=> current source-representation lane remains dependency-limited
```

This does **not** imply that no source law exists. It does mean arbitrary further linear source regrouping is not the default next route unless independent mathematics singles out the new representation.

## Current active obstruction

The live mathematical bottleneck after #201 is still FB-05, not a new claim ID:

```text
bounded direct parity orientation established
+
no structural arbitrary-state mechanism yet isolated
+
no opposing first-bad contact orientation yet composed on the same state

=> E4A4-SCHUR-FB-05 remains OPEN
```

The next research object should preserve the assembled canonical parity state rather than further split its source channels.

The first candidate family is the normalized parity gap

```text
E = e / ||W_e||^2
O = o / ||W_o||^2
G = O - E
Jbar = O' E - E' O = E G' - E' G.
```

The factorization is DERIVED. The obstruction only narrows if a predeclared independent sign law for its components survives falsification.

## Routing consequence

```text
STOP:
  more precision/depth on the same source graphs
  another selector/refit
  another arbitrary source-parenthesis change
  counting equivalent ratio/J formulations as independent constraints

NEXT:
  one full-composite Pair-A falsifier
  exact-center kill-switch first
  unchanged #197 cover only if the center law survives

IF ALSO UNRESOLVED:
  stop Pair-A representation engineering
  increase priority of Pair D / Pair B
```

## Claim firewall

- theorem authority remains PR #184;
- PR #197 remains the direct bounded `J_POSITIVE` certificate;
- PR #199 remains `SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED`;
- PR #201 advances latest research evidence to `DISCREPANCY_REPRESENTATION_DEPENDENCY_UNRESOLVED`;
- no new obstruction entry is being treated as theorem authority;
- FB-05 remains OPEN;
- negative-root exclusion remains OPEN;
- **RH remains OPEN.**