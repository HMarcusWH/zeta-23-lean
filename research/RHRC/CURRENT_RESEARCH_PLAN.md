# RHRC current research plan

> **Claim firewall: RH remains OPEN.**

## Authority split

Live GitHub head + exact compiler/CI evidence are authoritative dynamically. Do not hard-code a docs merge as permanent `live main` state.

### Theorem authority

```text
latest theorem-bearing PR = #163
validated theorem head = b418ff034428f92594bab0e5b8276181a086ee4b
validated theorem tree = c397b3a015ea54e38ecfe626d6e29556fe963839
RHRC #1039 = SUCCESS
Permansson #812 = SUCCESS
```

### Latest research-evidence anchor

```text
latest merged research PR = #170
validated research head = 70689692d5b92252bf9da97740385aaced2bf197
merged research commit = c94242fe41ae62b59aaa392a33e35034eb2c1b1e
research tree = 38e38a1d4cf90afa0e8103e58d1cbae626ebdeef
RHRC #1058 = SUCCESS
Permansson #831 = SUCCESS
```

### Control authority

```text
control-plane semantic anchor = PR #117
selected formal first break = E4A4-SCHUR-FB-05
terminal claim = RH_OPEN
```

Research PRs #165-#170 do not advance theorem or machine-claim authority.

## One-screen frontier

```text
PROVED THROUGH #163
  off-line zero
  -> retained regular cell-minimal first-bad certificate
  -> exact negative canonical source channel
  -> exact finite pole-prime discrepancy
  -> legal production Riesz 6 / even 8
  -> retained transformed negativity
  -> exact moment-prefix jets and signed Riesz boundary recurrence
  -> same-state shifted Riesz x cross-parity source obstruction
  -> exact quadratic-normal mixed source observable
  -> h^(7)(0) = -2*(2*pi)^6*M4
  -> finite-prime term samples the same observable
  -> exact R8-R9 = endpoint scalar * squared mixed seventh jet

POST-THEOREM FB-05 RESEARCH
  #165  exact S8 executable arithmetic audited
        broad finite positivity signal; no sign theorem
        positivity alone is not a contradiction

  #166  true shifted secular state executable
        u_lambda = c - W(H-lambda G)^(-1)r
        672-state scout; no negative successor found
        Q16/N3/K4/odd near-critical family isolated

  #167  Q16 full-cell scalar barrier attack
        no floating negative point found
        apparent minimum moves toward log 17
        direct whole-cell Arb: 256/256 leaves UNRESOLVED
        brute subdivision route rejected in current representation

  #168  log17 boundary-flat prime-entry jet
        exact executable K=2..8 identities
        odd first surviving order 7 ~ M3^2
        even first surviving order 9 ~ M4^2
        threshold + 18/18 two-sided Arb points positive
        Q17 = CONTINUES_DOWN_BUT_POSITIVE

  #170  theorem-aligned [W|c] Schur visibility/background audit
        unit-shell pivot matches post-#150 selected residual
        q17 threshold direction Schur-visible: rho != 0 finite-certified
        exact entering-q pivot effect positive at 7/7 checked offsets
        q-removed background central finite difference negative at 7/7 scales
        97/97 sampled physical states remain H1-aligned through q19
        0 sampled negative pivots
        no interval/barrier theorem

NOW — FB-05 THRESHOLD-TO-THRESHOLD SCHUR BARRIER
  use the local Schur-envelope identity
    P'(L) = u_L^T M'(L) u_L
  without assuming global monotonicity

  determine / bound
    integrated background loss between genuine arithmetic seams
    exact pole / arch / active-prime / scalar derivative cancellation
    arithmetic replenishment at threshold entry
    parity dependence
    whether P can reach zero while H1 remains valid
    whether a genuine theorem-relevant barrier survives broad falsification

AFTER
  weakest surviving arithmetic theorem
  -> compose with exact #161/#163 retained state
  -> FB-06 same-state contradiction / negative-root exclusion
  -> FB-07 outside-strip/trivial-zero seam + Mathlib RH wrapper
```

## Exact theorem package through #163

For the retained even-selected branch, Lean proves on one exact shifted negative state:

```text
R8(u_lambda) < 0
F_odd(lambda) = Gamma(lambda)*explicitCanonicalSourceMoment(u_lambda)
odd successor bad OR explicitCanonicalSourceMoment(u_lambda) != 0
h^(7)(0) = -2*(2*pi)^6*M4(u_lambda)
2*(2*pi)^4*(R8(u_lambda)-R9(u_lambda)) = S8(L)*|h^(7)(0)|^2
2*(2*pi)^4*R9(u_lambda) < -S8(L)*|h^(7)(0)|^2
```

The strict R9 inequality uses no sign assumption on `S8(L)`.

## What remains formally open

```text
explicitCanonicalSourceMoment != 0 -> M4 != 0
M4 != 0 -> explicitCanonicalSourceMoment != 0
finite production samples determine h^(7)(0)
canonicalPolePrimeRieszEndpointScalar L 8 >= 0
canonicalPolePrimeRieszEndpointScalar L 8 != 0
threshold-entry law as a separately Lean-theoremized aperture theorem
Schur-envelope derivative as a project Lean theorem
threshold-to-threshold Schur barrier / integrated-loss inequality
simultaneous even/odd bad exclusion
odd-selected first-bad closure
negative-root exclusion
RiemannHypothesis
```

## Reclassified FB-05 subroutes

### A. Threshold-to-threshold Schur barrier — NEXT

The theorem-aligned one-step Schur pivot and threshold visibility are now executable research infrastructure after #170. The next question is not to reconstruct the pivot again. It is whether the local envelope derivative can control the integrated loss of the positive H1 barrier from one nonzero von-Mangoldt seam to the next, while preserving exact channel cancellation.

This is the current highest-information route.

### B. Full scalar pivot / background decomposition — VALIDATED RESEARCH MECHANISM

#170 reconstructs the theorem-aligned `[W|c]` pivot, checks the rank-one Schur update and directional derivative exactly in the executable layer, certifies q17 visibility, separates the entering atom from the smooth background, and finds opposite local motions. It does not prove a whole-cell sign.

### C. Endpoint scalar `S8` — OPEN, lower standalone leverage

#165 found no finite sign reversal in the audited range, but no global theorem follows. Even if `S8 >= 0`, #163 gives `R9 <= R8 < 0`; sign alone is not a contradiction.

### D. SourceMoment / local jet rigidity — OPEN

#163 proves a shared analytic observable. #166-#170 do not establish a global sourceMoment-to-`M4` implication or converse.

### E. Simultaneous parity route — OPEN

#161 still allows the opposite parity to be bad. The parity-asymmetric threshold jets may become useful only when composed with a full canonical barrier restriction.

### F. Odd-selected coverage — OPEN

There is no WLOG-even theorem because `D` is algebraic, not unitary/isometric.

## Falsification requirements for the next research PR

- remain in theorem-aligned `[W|c]` one-step geometry;
- restrict first-bad interpretation to H1 predecessor-positive states;
- preserve von Mangoldt prime powers, not primes only;
- use the envelope identity locally and do not assume a global derivative sign;
- preserve exact pole/arch/prime/scalar cancellation in derivative attribution;
- compare integrated barrier loss with actual threshold replenishment;
- test multiple genuine arithmetic seams, sizes and both parities;
- search deliberately for `rho = 0`, weak visibility, adverse kicks, or barrier crossings;
- treat finite-difference evidence as finite-difference evidence, not derivative theorem authority;
- treat `UNRESOLVED` as unresolved, never as sign evidence;
- if an actual finite bad successor appears, feed it immediately into the #166 shifted-state discriminator.

## Dead/quarantined shortcuts relevant now

- pointwise fixed-sign smoothed-integrand positivity remains dead;
- generic first-bad geometry without canonical arithmetic remains insufficient;
- universal one-step domination is not research progress when obtained by restating successor positivity;
- global Loewner or minimizing-Schur monotonicity remains quarantined;
- brute direct whole-cell Arb subdivision in the current dependency-heavy representation is not the next certification strategy;
- independent channel Schur pivots may not be summed because the Schur map is nonlinear.

## Highest-leverage next move

Build a research PR for the **threshold-to-threshold Schur barrier**: use the local envelope derivative to estimate/certify integrated background loss and arithmetic replenishment across genuine von-Mangoldt intervals, then try to falsify that mechanism across q/N/parity before any new Lean investment.

Standing question:

> Given everything theoremized through #163 and learned experimentally through #170, can exact canonical arithmetic keep the theorem-aligned H1 Schur barrier strictly positive from one arithmetic seam to the next without assuming global monotonicity?

**RH remains OPEN.**
