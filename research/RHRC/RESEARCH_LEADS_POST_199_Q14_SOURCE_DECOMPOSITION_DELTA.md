# Research leads delta after PR #199 — Q14 source decomposition

> **RH remains OPEN.**
>
> This delta records the post-green interpretation of merged research PR #199. It is research-routing evidence, not Lean theorem authority.

## Exact anchors

```text
THEOREM AUTHORITY
PR #184
validated head = a756494ebe7e2530715e996b9a9a341fbe07c683
merge = 6f04e94473eb112b59eeaa2c6fc6ac6cbe7fe30e
tree = 6c77cd470809959a403b3bcc5f08d39f4076fa4c

UNDERLYING BOUNDED SIGN CERTIFICATE
PR #197
validated head = 2d936f9764abdfeaa82127d5c834c6c3e429da25
merge = 162df6ce8bc13a816937d747f2965bff6764fad0
tree = 97f6372a4c7c131006b4abc090c86767b9e99990
disposition = GLOBAL_MONOTONE_ORIENTATION / J_POSITIVE

LATEST RESEARCH EVIDENCE
PR #199
validated head = fabe301c95277345f0efe764252ce1c1213a4112
merge = 27dda545b7ccdb2870088ebaf317d85e3d999555
tree = 85eb8ea25d240c4a9c339262bdc611fae881f7f0
disposition = SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED
```

# What became formally true

No new Lean declaration was added by PR #199. Theorem authority therefore remains PR #184.

The formal theorem inventory still includes the complex-Hermitian Schur/contact calculus, the frozen production log-cover family, exact fixed-cell attachment, N2 predecessor/canonical-shell geometry, and the conditional orientation law

```text
P_t' = -envelopeNormSq + remainderEnvelopeDerivative
remainderEnvelopeDerivative < envelopeNormSq -> P_t' < 0.
```

The pole-prime discrepancy interface proved earlier remains available as theorem-backed cancellation-preserving infrastructure. No theorem says that this discrepancy forces the parity-Wronskian sign.

# What became research-certified

PR #199 first replays the exact completed PR #197 cover:

```text
post197_classification = GLOBAL_MONOTONE_ORIENTATION
post197_uniform_orientation = J_POSITIVE
completed_leaf_count = 49
```

It then validates the four-way canonical source split through second aperture order:

```text
pole
arch_signed
prime_signed
scalar_shift
```

and the collapsed direct-production representation:

```text
pole
direct_arch_signed
prime_signed
```

For both representations it requires reconstruction overlap with the independent direct Method-C transport. The scalar-shift self-Wronskian zero identity is also checked.

The final mechanism classification is:

```text
mechanism_classification = SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED
four_way_classification = SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED
collapsed_three_way_classification = SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED
selected_collapsed_uniform_lock_group = null
control_transfer_status = NO_UNIQUE_COLLAPSED_UNIFORM_LOCK
```

The direct bounded `J_POSITIVE` certificate remains intact.

# What changed

Before #199 the live question was:

> Which pole/arch/prime source interaction explains the complete positive Q14 orientation?

After #199 we know that the first independent source-interaction graph does not retain enough interval correlation to certify the sign of the already-signed direct quantity.

Thus the next question is narrower:

> Which cancellation-preserving representation, if any, exposes the structural relation without destroying it during enclosure?

This is a representation diagnosis, not a sign reversal.

# Upstream implications

The result increases the value of upstream definitions that preserve cancellation before interval transport.

The first theorem-motivated candidate is the pole-prime discrepancy:

```text
D = pole + prime_signed
```

with the remaining direct archimedean channel

```text
A = direct_arch_signed
```

so that

```text
M = D + A.
```

The critical implementation rule is that `D,D',D''` must be formed before parity restriction and before interval transport. Adding already-enclosed pole and prime Wronskian intervals afterward would repeat the #199 dependency loss.

This also strengthens the rationale for keeping exact source identities and sign conventions centralized rather than duplicating them downstream.

# Downstream implications

If the discrepancy-first representation recovers a stable sign mechanism, the downstream chain is:

```text
cancellation-preserving source relation
  -> parity Wronskian ordering
  -> parity-ratio monotonicity/injectivity
  -> broader reflected-twin exclusion
  -> attach to retained first-bad/contact state
  -> compose with #184 contact calculus
  -> same-state incompatibility candidate
```

If it remains dependency-unresolved, then further source splitting should stop. The next route should instead search for a higher-level exact composite parity identity or increase priority on alternative incompatibility pairs B/D.

# Resurrected routes

No dead route is resurrected wholesale.

DR-020 remains quarantined: loose independent source-channel majorants are still disallowed. What changes is that its declared escape requirement — exact paired-channel cancellation — now has stronger motivation from #199.

The post-#153 pole-prime discrepancy machinery therefore becomes a higher-priority representation candidate, not a revived coarse-channel strategy.

# New RH-relevant clues

## LEAD / HYPOTHESIS — discrepancy-first parity mechanism

Define at source/matrix jet level:

```text
D = pole + prime_signed
A = direct_arch_signed
```

then construct

```text
D, D', D''
A, A', A''
```

before parity restriction.

After restricting to normalized even/odd N2 predecessors, form only the three two-channel Wronskian interaction groups:

```text
J_DD
J_DA
J_AA
```

The hypothesis is that preserving the pole-prime cancellation upstream exposes a stable mechanism that the independent #199 source boxes destroyed.

This is not yet a theorem and must not be treated as one.

# Falsification checks

The next experiment should fail closed against the following:

1. **Same-state check:** exact #197 Q14 leaf partition, same Q/N/K/parity and same canonical lineage.
2. **No search expansion:** no new Q/N/K/parity population.
3. **No precision rescue:** keep the inherited Arb precision unless a separate implementation-check reason requires otherwise.
4. **No adaptive-budget rescue:** do not increase the inherited cover budget.
5. **No target leakage:** source classification must not import the direct `J_POSITIVE` label.
6. **Upstream pairing check:** `D` must be formed before interval transport.
7. **Reconstruction check:** `J_DD + J_DA + J_AA` must overlap independent direct Method C on every inherited leaf.
8. **Control blindness:** Q13/Q15 or other controls may falsify a selected mechanism but may not choose it.
9. **Mustache test:** any theorem candidate must be checked for equivalence to successor positivity, FB-05, or RH.
10. **Bounded/global firewall:** success on frozen Q14 is not arbitrary-state theorem authority.

Fast falsifier:

> if discrepancy-first pairing remains `SOURCE_DECOMPOSITION_DEPENDENCY_UNRESOLVED`, stop source chopping rather than adding more source subchannels.

# Highest-leverage next moves

```text
1. synchronize living docs/control through #199
2. implement discrepancy-first D/A source jets on exact inherited #197 cover
3. validate D,D',D'' independently against existing total/source backends
4. require exact two-channel Wronskian reconstruction of direct Method C
5. classify the complete Q14 hull without fitted thresholds
6. apply controls only after mechanism selection
7. if robust, isolate the smallest exact theorem candidate
8. test whether its hypotheses hold on the arbitrary retained first-bad/contact state
9. if still dependency-unresolved, stop source decomposition and search for a higher-level composite invariant
```

# Standing questions

> Given everything that is now formally true, what becomes possible that was not possible before?

The project can now distinguish a robust direct parity-ordering certificate from a failed attribution representation. This allows representation design to be guided by exact theorem-backed cancellation structure rather than by another selector or numerical refinement.

> If this contains a clue toward RH, where does that clue propagate upstream or downstream through the existing mathematics?

Upstream it points toward the pole-prime discrepancy interface and cancellation-preserving source definitions. Downstream it would propagate through parity-ratio monotonicity, reflected-twin exclusion, and the same-state FB-05 incompatibility programme.

> What experiment, lemma, reformulation, or connection would most efficiently tell us whether that clue is real?

Build the discrepancy-first two-channel representation on the exact frozen #197 cover and demand independent direct reconstruction before interpreting any sign.

**RH remains OPEN.**