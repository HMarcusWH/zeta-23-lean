# FB-05 incompatibility program — same-state composition instead of one miracle lemma

> **Claim firewall: RH remains OPEN.**
>
> This is a research-routing program. It records proved inputs, rigorous finite evidence, executable countermodels, candidate incompatibility pairs and falsification tests. It does not promote research output into Lean theorem authority.

## Core search rule

FB-05 should target a **same-state incompatibility**:

```text
RH false
  -> exists one exact retained canonical state x
  -> A(x)
  -> B(x)

prove independently:
  A(x) -> C(x)
  B(x) -> not C(x)

therefore x cannot exist.
```

Permanent composition rule:

```text
same state
same aperture
same parity
same normalization
same production object
```

Properties of different witnesses do not contradict each other until a theorem identifies the objects strongly enough to compose them.

## Existing theorem inputs

### PROVED — retained/source/Riesz state through #163

The retained selected state carries exact negative canonical source energy, transformed Riesz negativity, cross-parity/source identities, mixed source jets and finite-prime sampling relationships in the scopes proved by Lean.

### PROVED — PR #182

Generic real 2x2 Schur/contact calculus, including `Delta_2=aP` and contact-local determinant/pivot derivative orientation transfer under H1.

### PROVED — PR #184

The actual complex-Hermitian production/log-cover package:

```text
P = d - |b|^2/a
M~(t) = -t I + R~(t)
exact fixed-cell equality with parityCompressedCanonical
N2 predecessor/canonical-shell reconstruction and orthogonality
P_t' = -E + R_t'
R_t' < E -> P_t' < 0
```

This closes the algebraic production interface but not the arithmetic sign premise.

## Permanent equivalence / external-research firewalls

The post-#190 reroute does not erase earlier reusable warnings:

- `ArithmeticSideSubexponential` is the PR #27 scalar equivalence wall: an apparently easier scalar upper-bound target can be RH wearing a different name.
- `negative-index` separation remains an independent structural input and should not be conflated with the current production-realizability route.
- The external 67.25% simple-critical-zero result is compatible with a sparse exceptional off-line set; percentage counting alone cannot exclude one exceptional quartet.
- Where two external results instantiate essentially the same quadratic extremal mechanism, **Do not count them as two independent** constraints.
- **Sparse-exception test:** any global exclusion argument based on density/percentage information must explicitly control a thin exceptional set.
- **Mustache test:** reject a proposed reduction if it is equivalent to RH, successor positivity, or another open target after notation is unpacked.
- Preserve the `coordinate` firewall: logarithmic `t=log L` derivatives and physical-aperture `L` derivatives differ by the factor `L`.

These remain research hygiene rules even though the immediate Pair-A bottleneck has moved to canonical production realizability.

## Research inputs

### #186 — broad Pair-A domination falsifier

The frozen Q14 exact-center panel returns `DOMINATION_SIGNAL_MIXED`: two positive domination margins and four negative. Broad/unconditional `R_t'<E` is therefore not a credible theorem target across the tested dangerous-state class.

### #188 — frozen selector audit

Seven normalization-safe strong selectors and three diagnostic mechanism components were evaluated on the frozen panel with anti-leakage and normalization firewalls.

### #189 — individual selector independence

Every frozen candidate separately is exactly separable from target sign in the audited ambient normalized algebra.

### #190 — joint selector independence

The entire seven-dimensional strong selector vector is exactly identical across a rational reflected pair with the same nonboundary threshold signature and opposite target sign. Thus every nonempty subset of those seven observables is also insufficient in the ambient model.

This is **not** a canonical arithmetic realizability theorem. The #190 code explicitly leaves canonical realizability false/unclaimed.

## Consequence for Pair A

The pre-#190 form

```text
find selector C
  -> C implies local domination
  -> #184 gives negative orientation
```

is no longer the right immediate programme if `C` is only a function of the frozen G1/P1/P2/C1-C4 observation surface.

The post-#190 form is:

```text
actual canonical production constraint C_prod
  -> forbids the ambient reflected branch or forces a useful contact property
  -> #184 contact/log-drift calculus
  -> independent first-bad property on the same retained state
  -> contradiction
```

### Pair A — canonical-realizability / contact-orientation clash — CONDITIONAL LEADING ROUTE

**Algebraic side:** PROVED through #184.

**Ambient selector side:** CONSUMED by #189/#190. Do not search combinations/refits of the same seven strong observables.

**Live missing object:** a production relation that holds because all channel/geometry coordinates arise from the same canonical aperture state.

The existing research backend already exhibits one candidate constraint:

```text
scalar_shift = 2*cCorrection'(L) I
arch_signed  = -arch_direct - scalar_shift
```

so the arch and scalar coordinates used in source-channel diagnostics are not independent under actual production. This is a **lead**, not yet a proof that every #190-style twin is impossible.

**Next research test:** progressively impose actual production constraints on the #190 reflected twin and identify the first exact layer where opposite-target realizability fails.

Required layers:

```text
0 ambient normalized algebra
1 four-way production source-channel coupling
2 exact scalar-aperture law
3 common direct-arch derivative
4 common Schur geometry x=b/a and envelope E
5 full same-L, same-Q, same-parity canonical source reconstruction
```

A search failure earns only `UNRESOLVED`; an exact identity may earn `EXACT_TWIN_EXCLUDED_BY_IDENTITY` in its declared model.

## Pair B — negative-index separation versus critical-line sampling rigidity

The negative/transverse Gram structure remains genuinely different from Pair A. The open target is a same-window/same-test lower sampling or frame bound strong enough to overwhelm any exceptional off-line contribution. Zero-density percentages alone are insufficient.

## Pair C — local first-bad event versus short-window zero rigidity

A first-bad/contact event is highly local. The open question is whether the specific retained canonical test can remain bad under sufficiently strong local zero information. Any external zero-density/short-interval theorem remains a lead until normalized to the exact project object.

## Pair D — two-parity squeeze

Cross-parity source transfer and the odd-bad/source-moment fork make simultaneous parity information a plausible independent restriction. But no theorem currently rules out simultaneous badness, and the algebraic parity map is not unitary/isometric.

## Pair E — off-line growth versus structured finite-energy control

Growth/rigidity remains a long-range alternative. It must avoid the known scalar-equivalence wall and must not smuggle RH back in as an assumed growth bound.

## Falsification rules

For any proposed post-#190 Pair-A constraint:

1. **Canonical-origin test:** it must come from actual production, not a fitted selector.
2. **Same-state test:** same aperture/parity/normalization/production object.
3. **Ambient-vs-image test:** distinguish identities valid on all normalized states from constraints valid only on the canonical image.
4. **Target-leakage test:** no `margin`, `rho`, full pivot derivative or target alias disguised as an invariant.
5. **Normalization test:** basis/shell rescaling cannot manufacture the effect.
6. **H1 test:** no interval Schur claim outside certified H1.
7. **Search-vs-proof test:** failure to find a twin is not nonexistence.
8. **Mustache test:** reject routes equivalent to RH or successor positivity under another name.

## Highest-leverage order

```text
1. canonical-realizability audit of #190 twin
2. if exact production relation kills twin -> formalize narrow relation
3. compose with #184 and same-state first-bad property
4. if full production still admits twin -> downgrade Pair A
5. then prioritize Pair B / D before reviving dead selector or universal-domination searches
```

## Permanent claim firewall

```text
#184 conditional sign theorem         PROVED
#186 broad domination                 FALSIFIED in tested finite scope
#189 individual selector sufficiency  CLOSED in ambient algebra
#190 joint selector sufficiency       CLOSED in ambient algebra
canonical production realizability    OPEN
contact contradiction                 OPEN
negative-root exclusion               OPEN
RH                                    OPEN
```

**RH remains OPEN.**