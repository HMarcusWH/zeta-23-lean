# RHRC — Riemann Hypothesis Route-Closure harness

RHRC is the research-side control plane for the HMWH Zeta23 fork.

It combines four roles without conflating them:

- **FFBBP-RH**: candidate discovery, null competition, commutation and transfer diagnostics;
- **Permansson-style interventions**: predeclared changes with held-fixed variables and candidate-aligned ablations;
- **OoL-MVS v2.7.6 route closure**: typed evidence semantics, structural route/boundary binding, provenance closure, dependency closure, integrated-route qualification and confirmatory freeze discipline;
- **Lean / comparator**: final mathematical certification after a candidate has become an exact theorem.

The terminal target is unconditional RH. The research harness itself is not evidence for RH.

## FFBBP reference contract — RUN42C closure

The preserved theory reference is **FFBBP Reference Solver Architecture v1.5.1**, with machine-readable implementation-closure overlays. The public v1.4.2 reference remains provenance for the architecture-level doctrine; it explicitly does not claim operational validation.

The previous RUN42B A0 profile is now **development lineage only**. A later audit found `CONFIRM_COVARIATE_TRANSDUCTION`: Sinkhorn membership balancing used TRAIN+SELECT+CONFIRM source rows, allowing CONFIRM source geometry to affect TRAIN memberships, fitted field coefficients and SELECT calibration.

The qualified finite-synthetic profile is now **RUN42C_A0_INDUCTIVE_FIREWALL**:

- prototypes and Sinkhorn column scaling are fitted on TRAIN only;
- SELECT and CONFIRM memberships are inductive row-normalized transforms under frozen TRAIN-derived geometry;
- target-derived lure features remain prohibited from certificate geometry;
- field fit occurs on TRAIN, family/existence calibration on SELECT, then family/shrink are frozen for one no-refit CONFIRM;
- transformed synthetic truth is scored projection-consistently under the same TRAIN-only background projection;
- unknown-field diagnostic admission still requires known-null, known-positive and matched-artifact controls;
- the claim cap remains finite-synthetic / diagnostic only.

The supplied RUN42C protocol is SHA-256 `c562a770a37521589742909f75aa522bdb76233c6f9206bd70039afb85e74622`. Local reexecution reproduced every scenario gate: each of five planted-field scenarios passed 5/5 seeds, and both W96-artifact and no-field worlds were rejected 5/5.

The supplied detached manifest attests output ZIP SHA-256 `e33f43dfe367ff25c7e7496d7080d405ea87044c2aa3609c549dcc97da9d8e46`. A clean local rerun reproduced the semantic gate outcomes but not the archive bytes: the supplied script accumulates several ledgers through parallel `as_completed()` order and emits a fresh `created_utc`. Byte-identical replay is therefore **not claimed** until output ordering/timestamps are canonicalized.

Machine-readable FFBBP references:

- `ffbbp/FFBBP_REFERENCE.json`
- `ffbbp/IMPLEMENTATION_CLOSURE_OVERLAY.json`
- `ffbbp/configs/run42c_a0_inductive_firewall_qualified_profile.json`
- `ffbbp/configs/rh_adapter_v1.json`
- `ffbbp/configs/run42b_a0_v1_5_qualified_profile.json` — historical/quarantined development lineage

The RUN42C lineage also records a remaining documentation gap: a future canonical v1.5.3 document should freeze/disclose the full synthetic scenario-generator, noise and background fixture contract. RHRC does not pretend that binary exists.

## OoL-MVS v2.7.6 operational contract + 2026-08-20 architecture overlay

RHRC remains bound to **OoL-MVS Kernel v2.7.6 (2026-08-19)**. The clean release uploaded again on 2026-08-20 is byte-identical to the already-bound release:

- clean release outer ZIP SHA-256: `3fa2d68e6ade43822021444af6573b5fa722d81af80370b26e7abf1f44b30ce3`
- embedded kernel ZIP SHA-256: `35eef74477b97cfa0f0c5367b51747022ba78c4df16e54869c41614d99e184f2`
- packaged kernel checks: **530/530 PASS**

The kernel binary therefore does not change. What changes is the top-level route architecture, recorded in `ool/OOL_ARCHITECTURE_OVERLAY_2026_08_20.json`:

- **Experiment A — Transition-Core Validation**: activated nonliving feed -> generated ensemble -> endogenous RE -> same-core PCS.
- **Experiment B — Full Route-Closure Experiment**: F0 geochemical sources -> B1 -> B2 -> B3 activation -> B4 generated ensemble -> B5 -> endogenous RE -> same-core PCS.
- The flagship full-origin laboratory claim belongs to B, not A.
- Before full-route confirmation, the actual route must pass: module qualification -> physical integration -> U-1…U-6 -> **FULL-ROUTE FREEZE** -> blinded confirmatory run.
- Independent success of every module is not end-to-end closure. Actual output must traverse every declared interface, and claim-bearing lineage must remain continuous.
- Boundary/debt claims are explicit: Tier A transition closure, Tier B full laboratory/deep-source closure, Tier C natural closure, Tier D historical inference.

The RH transplant is domain-neutral: local lemmas/boxes can all be correct while the global proof route is still open. Confirmatory freeze is eligible only after every theorem-relevant interface has been qualified in the actual route and admitted provenance remains continuous end to end.

The pre-existing v2.7.6 laws remain:

- complete synthetic/adversarial worlds may use two-valued `TRUE/FALSE`, but cannot issue a research/scientific certificate;
- partial research evidence uses strong-Kleene `PASS/FAIL/NA`; missing or unresolved evidence is `NA`;
- `ClaimResult` is separate from `CertificateStatus`;
- claim-bearing evaluations bind raw evidence and exact support;
- unknown provenance is not admitted provenance;
- absence claims require a complete declared search/provenance domain;
- discovery may adapt, but confirmatory execution requires frozen route and boundary digests;
- post-unblinding claim-bearing changes require a new route digest and fresh confirmation.

RH imports governance/evidence semantics only. Origin-of-life chemistry, empirical success, natural closure and historical occurrence are **not** RH premises.

## Main routes

- `R001_exceptional_zero`: seek a multi-scale observable for which one off-line zero forces positive exponential growth and seek an unconditional prime-side upper bound. The zero side is now closed sorry-free in Lean (exposed-pole detector, exact Weil/translation bridge, pole-killer, target-adaptive visibility, reflection assembly), and the scalar prime-side target is proved logically equivalent to RH itself (`arithmeticSideSubexponential_iff_criticalLine`; OBS-008). Terminal state: RH_OPEN with exactly one remaining theorem.
- `R002_multi_probe`: test whether a family of probes separates structures that one finite compression cannot distinguish. The negative-index channel of the windowed Gram now does so at block level, sorry-free in Lean (`R002_MULTI_PROBE_SEPARATION`, `Zeta23/ExceptionalZero/ProbeGramNegativity.lean`): a visible off-line pair block is not equal to any nonnegatively weighted sum of real rank-one blocks — the shape of every on-line configuration and of the TightMult mock. Windowed visibility and the arithmetic leg remain OPEN (OBS-009: the latter is band-limited Weil positivity, RH-equivalent).
- `R003_ccm_bridge`: require an exact identity/intertwining/controlled limit before connecting finite CCM objects to Weil/Suzuki-style objects.

Current registered theorem routes remain in `DISCOVERY` until a real route is ready to freeze.

## Promotion rule

`research/RHRC` -> exact conjecture -> falsification/countermodel pass -> integrated route/dependency closure -> sorry-free Lean -> axiom audit -> comparator topic.

OoL evidence certificates govern integrity of the **research route**. FFBBP governs diagnostic admission. Neither replaces Lean/comparator as the authority for mathematical theoremhood.
