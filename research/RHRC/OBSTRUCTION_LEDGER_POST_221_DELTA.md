# Obstruction-ledger delta after validated PR #221 theorem stage

> **Claim firewall: RH remains OPEN.**
>
> Merged theorem authority is PR #220. Exact PR #221 theorem head
> `20018c931f4516432ace5bd06788276be656641b` is compiler/no-placeholder
> validated but not yet merged in this snapshot.

## OBS-059I — branch-classified after exact zero-shift Schur classification

**Status:** OPEN / BRANCH-CLASSIFIED / CANONICAL-ARITHMETIC-DISCRIMINATION REQUIRED.

The validated #221 delta consumes one important ambiguity: successor badness is
no longer an undifferentiated event. Under predecessor nonnegativity it is
exactly

```text
actual shell coupling resonant
  K(b) != 0

or

actual shell coupling regular with negative zero-shift endpoint
  exists x0, A x0 = b and re S0(x0) < 0.
```

Successor goodness is exactly the complementary regular/nonnegative case.

### What this closes

```text
opaque good-sector zero-shift solvability hypothesis
opaque simultaneous odd-bad branch geometry
uncertainty whether regular-negative and resonant badness exhaust the one-step mechanisms
```

### What remains open

```text
arithmetic exclusion of actual-shell resonance
arithmetic exclusion of the regular-negative endpoint branch
relation, if any, between K(b), #219 K(a), and #220 K(d)
nontriviality of K(d) on retained canonical states
exact pole/range resolvent cost decomposition
zero-shift <-> negative-shift observable bridge
odd-selected first-bad closure
negative-root exclusion
finite obstruction exclusion
finite-to-zeta closure
RiemannHypothesis
```

## Same-state and coordinate firewall

`cubicCouplingKernelPart` is the actual shell-coupling kernel coordinate.
It must not be silently identified with `oddCubicGeneratorKernelPart` from
#219 or `oddIndexCubicShellKernelPart` from #220.

A generic Hermitian block system can realize regular-negative or resonant
badness. The classification is therefore structural compression, not an
arithmetic contradiction.

## Current next target

`RETAINED_BRANCH_ARITHMETIC_DISCRIMINATION`.

R003 remains `DISCOVERY`; confirmatory execution is not authorized.
The PR #117 control-semantic anchor, action scores, first-break IDs, claim IDs,
and route/boundary digests remain unchanged.

**RH remains OPEN.**
