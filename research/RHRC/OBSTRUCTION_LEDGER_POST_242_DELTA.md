# Post-#242 obstruction ledger delta

## What became formally true

PR #242 closes the terminal Mathlib statement seam:

```text
NoRegularFirstBadCertificates
  -> Mathlib.RiemannHypothesis
```

This implication is PROVED. Its antecedent is OPEN.

PR #239 already proves generic safe negative-shift transfer reality and retained
real Gamma/Alpha geometry. PR #240 proves the retained Gamma obstruction normal
form and the conditional `D > 0` transfer escape classification.

## Workflow harvest

11/11 workflows attached to the validated #242 head completed successfully.
Research replays preserve prior dispositions and promote no theorem.

## What changed

`OBS-059I` is now compressed to:

```text
safe-shift transfer reality                 CLOSED / #239
retained Gamma/Alpha real geometry          CLOSED / #239
Gamma/Alpha obstruction normal form         CLOSED / #240
terminal Mathlib RH seam                    CLOSED / #242

NoRegularFirstBadCertificates               OPEN / strong sufficient endpoint
off-line-generated whole-cell provenance    OPEN / NEXT THEOREMIZATION
D > 0                                       OPEN
selected-even odd-good contradiction        OPEN
simultaneous odd-bad exclusion              OPEN
odd-selected downstream orientation         OPEN
parity-complete retained-state exclusion    OPEN
RH                                           OPEN
```

No new OBS identifier is introduced.

## Upstream implications

The terminal API currently forgets eventual badness at every sufficiently large
aperture. The next theorem target is to preserve that family property through
bi-regular retained selection.

## Downstream implications

If whole-cell/arbitrary-large-aperture provenance is formalized, subsequent
closure attempts may use generated-family information rather than proving
universal inequalities for arbitrary retained certificates.

## Resurrected routes

The contact-locus route is reclassified from "missing same-state bridge with no
source" to **RESURRECTED LEAD FROM WHOLE-CELL PROVENANCE**. No contact theorem is
claimed.

## New RH-relevant clues

A hypothetical off-line zero may force a family of retained states at arbitrarily
large aperture. That family is a stronger target than one isolated certificate
and may constrain the #240 scalar regimes.

## Falsification checks

Whole-cell badness allows the finite bad size to vary with aperture. It does not
supply persistence of one fixed witness, one fixed `K`, a contact point, or a
large-`L` sign theorem.

## Highest-leverage next moves

Formalize `OFFLINE_GENERATED_WHOLE_CELL_RETAINED_FAMILY` and preserve the
claim firewall. Do not attack the universal #242 antecedent merely because it is
sufficient.

## Standing questions

Can the cell-minimal bad size persist across the whole generated cell?
If not, can a boundary/contact state be extracted?
If yes, what canonical arithmetic forbids whole-cell persistence?
What do the #240 scalar invariants do along arbitrarily far-out generated cells?

**RH remains OPEN.**
