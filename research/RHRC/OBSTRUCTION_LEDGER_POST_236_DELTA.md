# Post-#236 obstruction-ledger delta — real-phase collapse

> **RH remains OPEN.**

## What became formally true

PR #236 closes the phase-freedom subproblem on the selected-even retained
negative-root state. The canonical trial, exact source moment, and source kernel
are conjugation-fixed. The exact #235 compatibility therefore becomes

```text
D <= 0 OR D^2 <= s^2 Rsharp
```

with one real source scalar `s`.

## Workflow harvest

Exact #236 head: `45491342f5661429679579c7889c1ad8b96728b6`.
Merge: `a66e1c617033f4adaa52e935668399efb93048ac`.
Tree: `5ef597b1d75075ec2299261a4193432a36932ffe`.
All 13 attached workflows completed green.

The main Lean/compiler gate is theorem authority. Research replays remain
non-promoted: post-200 is dependency-unresolved, post-202 preserves the
falsified composite-gap pattern, post-214 preserves ambient sign indefiniteness,
and post-222 still does not reach a certified retained biregular state.

## What changed

**Closed:** free complex phase of the retained canonical source.

**Still open:** sign and magnitude control strong enough to violate the
one-real-source radius compatibility.

The obstruction has therefore changed from complex-plane geometry to a real
semialgebraic feasibility problem.

## Upstream implications

No stronger matrix reality theorem is needed. Existing #236 conjugation
infrastructure already proves centered moments commute with coordinate
conjugation. The next upstream lemma is simply retained `M4` reality.

## Downstream implications

The exact completed source `F=M4-S*C` was previously eliminated to obtain an
M4-free compatibility law. With `S` now real and `C` already real,
the next route should retain `F` and prove the scalar package

```text
B > 0
q > 0
c >= 0
B <= s*f
(q-f)^2 <= Rsharp
B + s^2*c <= s*m
D <= s*(f-q)
```

where `f=m-s*c`.

## Resurrected routes

The completed-source route is re-opened in a stronger form because a dependency
changed: canonical phase reality is now proved. This does not revive the dead
post-202 composite parity-gap pattern and does not revive universal ambient
source-sign arguments.

## New RH-relevant clues

**LEAD / HYPOTHESIS:** `Rsharp <= q^2` would force `s>0` on the odd-good
branch once the real corridor is theoremized. This could propagate into the
deficit/radius contradiction interface from #236.

**OPEN:** whether the exact retained canonical arithmetic actually forces that
barrier.

## Falsification checks

- Do not infer the barrier from nonnegativity of its factors alone.
- Preserve the sharp negative-shift correction.
- Check equality/tangency and zero cases.
- Compare with historical bounded fixtures before escalating the clue.
- Keep selected-even distinct from odd-selected; no WLOG step is licensed.

## Highest-leverage next moves

The next theorem target is `RETAINED_REAL_COMPLETED_SOURCE_CORRIDOR`.
Required new information:
`REALITY_OF_RETAINED_M4_AND_SCALAR_COMPOSITION`.

After that, the highest-information mathematical attack is the retained
sharp-radius/shell barrier, not another representation regrouping.

## Standing questions

**What becomes possible now?** The completed-source object can be kept instead
of eliminated while still remaining one-dimensional over the reals.

**Where can the clue propagate?** Into source sign, moment-four sign,
center-deficit orientation, and ultimately selected-even odd-good exclusion.

**What test is cheapest?** Formalize the corridor, then directly inspect the
exact expression for `Rsharp-q^2` and try to prove or falsify its sign.
