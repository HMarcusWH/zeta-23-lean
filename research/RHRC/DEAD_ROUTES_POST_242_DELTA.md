# Post-#242 dead-route delta

## What became formally true

PR #242 proves the conditional terminal implication to Mathlib's exact
`RiemannHypothesis`. The terminal statement seam is therefore no longer an
open research route.

## Workflow harvest

All 11 attached #242 workflows completed successfully. No research replay
promotes a theorem.

## What changed

### Closed / no longer worth spending theorem PRs on

- generic safe-shift transfer reality — CLOSED by #239;
- retained Gamma/Alpha reality — CLOSED by #239;
- construction of a terminal Mathlib statement seam — CLOSED by #242;
- Bernoulli/special-value terminal cleanup via
  `riemannZeta_two_mul_nat` on the pinned Mathlib revision — unsuitable for
  promoted no-sorry ancestry because that dependency exposes `sorryAx`.

The completed-zeta/Gamma-zero-classification route is the validated replacement
for the negative-odd-integer seam.

### Still dead / invalid

- reality alone -> Gamma positivity;
- generic predecessor/parity/shell/displacement structure -> simultaneous-bad exclusion;
- analytic zero-side evenization -> selected reversal parity must be even;
- universal arbitrary-certificate exclusion inferred from the #240 scalar laws;
- numerical/frozen Q14 evidence -> arbitrary-Q theorem authority.

## Upstream implications

The universal terminal antecedent is stronger than necessary. Provenance should
be preserved before strengthening finite inequalities.

## Downstream implications

The next closure candidate should be generated-family-specific:
whole-cell badness and arbitrary-large-aperture ancestry composed with the
existing bi-regular retained state and #239/#240 geometry.

## Resurrected routes

### Contact / bad-locus boundary — RESURRECTED LEAD

The old failure reason was lack of a same-state contact bridge. Whole-cell
off-line-generated ancestry creates a new possible source of boundary geometry,
but no zero/contact theorem is currently proved.

### Large-aperture orientation — RESURRECTED LEAD

Off-line zero -> badness for all sufficiently large apertures means a useful
large-`L` orientation of `D`, `Gamma`, `Alpha`, `Rsharp/q^2`, or a
Riesz endpoint factor need only hold along the generated family, not over every
abstract certificate.

## New RH-relevant clues

The missing theorem may be family-specific rather than universal.

## Falsification checks

A changing bad size can evade naive continuity arguments. A boundary of an
existential bad set does not automatically supply a continuous eigenvector or
the exact Schur contact required downstream.

## Highest-leverage next moves

Preserve whole-cell/arbitrary-large-`L` provenance in Lean, then attack the
contact-or-persistence dichotomy.

## Standing questions

Does the generated family force one finite size to remain bad on a whole cell?
Can compactness/minimality/continuity extract a contact without choosing a
continuous eigenvector?
Which scalar has the cheapest canonical large-`L` asymptotic?

**RH remains OPEN.**
