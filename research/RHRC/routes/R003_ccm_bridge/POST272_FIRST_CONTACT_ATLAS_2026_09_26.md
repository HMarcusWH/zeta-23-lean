# Post-#272 true-ground first-contact atlas

**Claim cap:** EXPERIMENTAL_SIGNAL_ONLY  
**Terminal claim:** RH_OPEN

## Why this experiment exists

PR #272 proves that `CofinalCanonicalArithmeticCertificates` is RH-equivalent.
Direct construction of that cofinal all-vector family is therefore no longer
treated as an independent intermediate lemma.

The live descriptive bottleneck returns to OBS-060: understand the unconditional
legal ground spectrum inside fixed cutoff cells and at real von-Mangoldt seams
before attempting another terminal criterion.

## Dumbassery correction

At a threshold `L = log(q)`, the entering q atom itself vanishes because its
basis weight is evaluated at the endpoint.  Therefore **there is no value jump
to discover at the seam**.

This atlas tests the one-sided response immediately to the right of the seam.
The physical canonical energy is compared with the smooth continuation obtained
by ablating the current-q atom.  q=14 and q=15 are exact `Lambda(q)=0`
negative controls, so the ablation must be an exact no-op there.

## Frozen schedule

Primary K=3 cases:

- true seams: q=13, 16, 17;
- zero-von-Mangoldt controls: q=14, 15.

Replication:

- (q,K)=(16,4), (17,4).

One-sided offsets are fixed at `2^-8`, `2^-10`, and `2^-12`.
No point is moved after looking at results.

The existing #249 zero-side perturbation is reused with gamma=10:

- on-line control: delta=0;
- off-line control: delta=1/20.

These are local zero-side falsifiers, **not** self-consistent alternative Euler
products or zeta functions.

## Multiplicity firewall

A parity branch is called strict only when Arb proves the even and odd ground
bottom enclosures are disjoint.  If they overlap, the result is
`PARITY_GAP_UNRESOLVED`; no canonical eigenvector is selected and no
Hellmann-Feynman inference is made.

## What a positive result means

`CERTIFIED_CURRENT_PRIME_GROUND_RESPONSE` means that at least one preregistered
true seam has a certified, consistent nonzero right-hand difference between the
physical canonical legal ground and the current-prime-ablated continuation at
at least two frozen scales, while the exact Lambda=0 controls remain no-ops.

It does **not** mean:

- first-crossing exclusion;
- global aperture monotonicity;
- an asymptotic law;
- negative-root exclusion;
- a theorem about all K or all seams;
- a proof of RH.

## Falsification value

If no certified response survives, or if the planted off-line control exhibits
the same local pattern without an arithmetic distinction, the first-contact seam
route is downgraded before another large Lean theorem campaign.

RH remains OPEN.
