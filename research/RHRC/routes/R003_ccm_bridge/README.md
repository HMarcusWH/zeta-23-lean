# R003 CCM bridge — active route

> **RH remains OPEN.**

## Authority

```text
Lean theorem authority = PR #184
latest research evidence = PR #197
control semantic authority = PR #117
route phase = DISCOVERY
```

PR #184 proves the complex-Hermitian production/log-cover/contact algebra. No later research PR promotes theorem authority.

## Preserved research ancestry

- PR #186: `DOMINATION_SIGNAL_MIXED`; broad universal production-remainder domination falsified in tested scope.
- PR #190: `JOINT_EXACT_VECTOR_SEPARABLE` / `JOINT_THRESHOLD_SIGNATURE_SEPARABLE`; all 127 nonempty selector subsets insufficient in ambient normalized algebra.
- PR #192: canonical production realizability Layer 0 -> Layer 5; specific reflected witness excluded, general reflected class `UNRESOLVED`.
- PR #193: positive exact Q14 centers but `TRAJECTORY_RIGIDITY_UNRESOLVED` over finite width.
- PR #195: validated `M,M',M''`; historical `PARTIAL_TRAJECTORY_ORIENTATION`, 48 `J_POSITIVE`, 48 `J_UNRESOLVED`, `63/64`, `global_positive_hull = false`.
- PR #197: unique inherited budget leaf replayed once; A unresolved, B/C positive; complete cover becomes `GLOBAL_MONOTONE_ORIENTATION`, uniform `J_POSITIVE`, certified fraction `1`, `global_positive_hull = true`, `bounded_distinct_aperture_twin_exclusion = true`.

## Current route question

The frozen Q14 bounded sign question is consumed. The live route is now a source-mechanism/generalization audit:

```text
J = o'e - e'o
J' = o''e - e''o
(o/e)' = J/e^2
(log(o/e))' = J/(oe)
```

Use the same canonical source lineage and decompose pole/arch/prime contributions to `J'`, retaining all bilinear cross-channel terms. Require rigorous reconstruction of direct `J'` before trusting any dominance explanation.

The goal is to discover or falsify an independently meaningful canonical arithmetic relation behind the complete Q14 orientation, not to fit a new selector or widen the search population.

## Claim firewall

`GLOBAL_MONOTONE_ORIENTATION` is rigorous bounded research on the exact frozen Q14 domain. It does not establish arbitrary-Q ordering, global canonical injectivity, FB-05 closure, negative-root exclusion or RH.

Theorem authority remains #184. **RH remains OPEN.**