# Post-#243 obstruction ledger delta

## What became formally true

PR #243 proves that any off-line zero produces whole-cell bi-regular retained
negative-energy certificates at arbitrarily large retained aperture.

PR #242 already proves the conditional terminal implication to Mathlib's exact
`RiemannHypothesis`.

Therefore the preferred RH-facing obstruction is now generated-family boundedness,
not universal retained-certificate exclusion.

## Workflow harvest

All 11 attached #243 workflows completed successfully. Research replays preserve
their frozen dispositions and promote no theorem.

## What changed

`OBS-059I` now has the following compressed terminal view:

```text
terminal Mathlib statement seam                      CLOSED / #242
whole-cell off-line provenance                       CLOSED / #243
arbitrarily-large generated retained family          CLOSED / #243

generated-family eventual aperture upper bound       OPEN / FINAL GATE
NoRegularFirstBadCertificates                         OPEN / stronger than needed
simultaneous odd-bad exclusion                        OPEN / not required if final gate closes
odd-selected downstream orientation                   OPEN / not required if final gate closes
RH                                                     OPEN
```

## Upstream implications

Any hypothetical off-line zero supplies a family extending beyond every real
aperture threshold. The final contradiction may therefore be eventual rather
than uniform.

## Downstream implications

An upper bound on retained aperture for
`WholeCellBiRegularNegativeEnergyCertificate` directly excludes off-line zeros
and composes through the #242 seam to RH.

## Resurrected routes

The large-aperture production decomposition is promoted to the first direct
falsification attempt.

The contact route remains fallback only.

## New RH-relevant clues

The final gate can be expressed as one boundedness proposition over the exact
generated certificate class.

## Falsification checks

The explicit `-log L` scalar contribution alone is not a contradiction.
All compensating production channels must be controlled on the same retained
state.

## Highest-leverage next moves

Prove or falsify the eventual retained-aperture bound. If the proof stops at one
uncontrolled exact term, isolate that term as the sole next theorem target.

## Standing questions

Which exact production term can offset the large-`L` drift on the generated
retained family?

Can it remain large enough for arbitrarily large retained aperture?

What is the smallest exact bound that rules that out?

**RH remains OPEN.**
