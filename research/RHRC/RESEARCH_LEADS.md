# RHRC living research leads ledger

> **Claim firewall: RH remains OPEN.**
>
> This is a research inventory, not theorem authority. Exact Lean/compiler/CI state wins.

## Current theorem / research authority

```text
THEOREM AUTHORITY
latest theorem-bearing PR = #182
validated theorem head = 0c3f63cdc4774ba1a68b21d1558ea0ee860a938d
merged theorem commit = a69160d37a84049711aaff6c3d5db804583a7306
validated theorem tree = e0b260b3b3a1ed470d54c14d8c0c46b32379f3fb
RHRC #1082 = SUCCESS
Permansson #855 = SUCCESS

LATEST RESEARCH-EVIDENCE ANCHOR
merged research PR = #180
validated research head = a87469da9e611b53ae400cb4b18ce4afeb94e6d2
merged research commit = 93ea3df51f2671e316197c1530ea504dee76e821
research tree = 8199cdc543f1b761c70466dfd602c143a277e6a2
RHRC #1077 = SUCCESS
Lean #837 = SUCCESS
Permansson #850 = SUCCESS

CONTROL AUTHORITY
control-plane semantic anchor = PR #117
selected first break = E4A4-SCHUR-FB-05

newest post-green delta = RESEARCH_LEADS_POST_182_SCHUR_ENVELOPE_CONTACT_ORIENTATION_DELTA.md
current execution SSOT = CURRENT_RESEARCH_PLAN.md
RH = OPEN
```

## Promoted theorem inputs

### Retained first-bad state — PROVED / #153

Whole-cell ancestry, regular selected predecessor, negative explicit root, exact predecessor solve and negative canonical source energy.

### Exact discrepancy + legal Riesz engine — PROVED / #153/#155

Pole/prime cancellation is theoremized before smoothing; legal left-anchored Riesz primitives and conditional repeated integration by parts are compiler-validated.

### Complex production transport + transformed negativity — PROVED / #157

Exact complex production D-transport, complete Riesz 6/even 8, retained transformed negativity, and the ExceptionalZero Riesz-6 wrapper.

### Exact moment-prefix jets + signed Riesz boundary — PROVED / #159

General moment-prefix odd-jet theorem, exact seventh/even-ninth self-energy leading terms, generic signed Riesz boundary recurrence, and retained decompositions.

### Same-state shifted Riesz x cross-parity source — PROVED / #161

The exact shifted negative trial is simultaneously tied to strict even R8 negativity and the cross-parity production source obstruction. Under opposite-parity goodness, the explicit source moment is nonzero.

### Mixed quadratic-normal jet x Riesz boundary — PROVED / #163

```text
h_v^(7)(0) = -2*(2*pi)^6*M4(v)
finite-prime term samples the same h_v
2*(2*pi)^4*(R8-R9) = S8(L)*|h_v^(7)(0)|^2
```

The same retained shifted state therefore has spectral, transformed-energy, global-source and local-analytic constraints.

### Generic Schur-envelope contact calculus — PROVED / #182

For the generic real 2x2 block:

```text
Delta_2 = aP
P' = d' - 2*(b/a)b' + (b/a)^2 a'
Delta_2' = a'P + aP'.
```

At contact `P=0`, Lean proves `Delta_2'=aP'`; under H1 `a>0`, determinant and pivot derivative orientations agree.

**Limit:** this supplies no production arithmetic sign and no contact existence theorem.

## Research evidence accumulated through #180

### #165 — endpoint scalar

Broad finite positive evidence survives, but endpoint-scalar positivity alone propagates transformed negativity rather than closing FB-05.

### #166 — shifted-state discriminator

Finite discovery was corrected to the true generalized shifted secular trial.

### #167 — Q16 full-cell method falsification

Dependency-heavy direct whole-matrix interval subdivision remained unresolved and was rejected as the current strategy.

### #168 — prime-power threshold jet

Boundary-flat parity analysis exposes high-order threshold jets; the exact Q17 microscope remained sampled positive.

### #170 — theorem-aligned Schur visibility/background split

The research scalar was moved to the exact `[W|c]` one-step geometry.

### #172 — threshold-to-threshold production barrier

Current-q arithmetic lift is sign-indefinite. q13/N2/K3/even became the strongest near-critical finite target.

### #174 — exact q13 2x2 scalar barrier

The dangerous finite target reduces to

```text
H(L) = [[a(L), b(L)],
        [b(L), d(L)]]
Delta_2(L)=a(L)d(L)-b(L)^2
H1 <-> a(L)>0.
```

Direct scalar interval subdivision remained unresolved.

### #176 — fixed-unit enclosure method selection

The fixed-unit pullback passed the frozen factor-2 determinant-width benchmark and remains the preferred finite enclosure representation.

### #178 — complete fixed-unit derivative discrimination

The complete fixed-Q derivative backend was validated, but all primary raw assembled derivative boxes remained `DERIVATIVE_UNRESOLVED`.

### #180 — exact-center derivative basin / Schur H1 scope

All six frozen primary exact centers certify

```text
POINT_DERIVATIVE_BASIN_BRACKETED
MINIMUM_ORIENTED
left 3/3 Delta_2' < 0
right 3/3 Delta_2' > 0.
```

The nonzero-width primary Schur graph is instead

```text
SCHUR_OUT_OF_H1_SCOPE
applicable_primary_count = 0.
```

So the first finite-width task is H1 recovery, not second derivatives.

## Active lead 1 — centered H1 recovery and finite-width derivative propagation

**Research status:** HIGHEST LEVERAGE / NEXT  
**Formal status:** OPEN

Run in this order:

```text
1. centered H1 recovery from point a(L0)>0 and rigorous a'(I)
2. if H1 succeeds, retry the theorem-backed #182 Schur derivative graph
3. if derivative sign remains unresolved, bound Delta_2'' and propagate the sharp point sign
4. only after signed left/right neighborhoods, use interval Newton/Krawczyk
```

The determinant and pivot minima remain distinct. #180 did not show the Schur graph is too wide; it was out of H1 scope.

## Active lead 2 — Pair-A production contact-orientation clash

**Research status:** HIGH LEVERAGE  
**Formal status:** generic calculus PROVED / production arithmetic OPEN

The fixed-cell theorem inventory already gives

```text
M_Q(L) = -log(L) I + R_Q(L)
```

through production compression. For orthogonal raw coordinates `W,c`, put `x=b/a` and `u=c-xW`. The universal drift should contribute

```text
a'_log = -||W||^2/L
b'_log = 0
d'_log = -||c||^2/L
P'_log = -||u||^2/L.
```

The desired theorem interface is therefore

```text
P'(L) = -||u_L||^2/L + remainder_drift(L,u_L).
```

Then seek/falsify a source-specific contact-local bound on `remainder_drift` that forces the orientation opposite to first-bad crossing.

### Bridge debt

- production is complex Hermitian while #182 is generic real 2x2;
- the q13 research integer shell generator spans the same one-dimensional shell as Lean's canonical cubic shell but is not theorem-identified at the same magnitude;
- the remainder currently has scalar-coordinate analytic infrastructure, not yet the exact packaged operator derivative needed by the slogan;
- moving-state terms must be handled by an exact envelope theorem, not dropped informally.

**Preferred formal route:** invariant/Hermitian first, then specialize to the real q13 coordinates.

## Active lead 3 — same-state incompatibility matrix

Maintain the program in `FB05_INCOMPATIBILITY_PROGRAM.md`.

Current leading pairs:

```text
A contact orientation clash                    leading
B negative-index vs localized sampling rigidity
C local first-bad vs short-window zero rigidity
D two-parity squeeze
E off-line growth vs structured finite-energy control
```

Pair A is currently cheapest because #182 has already consumed the generic contact calculus.

## Consumed mechanisms

- fixed-unit value-level enclosure — #176;
- complete analytic derivative backend — #178;
- exact-center point derivative scout — #180;
- generic real 2x2 Schur-envelope calculus — #182;
- threshold-to-threshold barrier accounting — #172;
- theorem-aligned pivot/background decomposition — #170.

Do not re-run consumed steps as if they were new mathematics.

## Falsification priorities

1. split universal `-log(L)` drift from the arithmetic remainder at the frozen #180 centers;
2. kill Pair A quickly if the remainder routinely overwhelms/reverses the universal drift;
3. test the invariant/Hermitian bridge before coding a q13-specific coordinate theorem;
4. preserve the sparse-exception and Mustache tests for zero-distribution/growth routes;
5. keep global Schur monotonicity quarantined.

## Highest-leverage next move

Run centered H1 recovery and the log-drift/remainder split in parallel. If the structural signal survives, theoremize the production/Hermitian envelope interface and only then invest in a contact-local arithmetic inequality.

**RH remains OPEN.**
