import Zeta23.PrimeSideA
import Zeta23.PrimeSideB
import Zeta23.ExceptionalZero.Defs

namespace Zeta23.ExceptionalZero

/-- The prime-side libraries are in the dependency cone. The sought unconditional residual bound
    remains OPEN in RHRC and is intentionally not represented by an axiom. -/
theorem prime_side_libraries_available : True := by trivial

end Zeta23.ExceptionalZero
